components:
  schemas:
    AuditEvent:
      additionalProperties: false
      properties:
        archon_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        correlation_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        created_at:
          format: date-time
          type: string
        id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        payload:
          additionalProperties: {}
          type: object
        source:
          type: string
        type:
          description: Audit event type, `<area>.<action>` (docs/naming-rules.md -> Audit-events). The enum is the full catalog keeper can write, generated from the audit event-type declarations; the catalog is open, so a new release may add values.
          enum:
            - apply.cancelled
            - apply.dispatched
            - audit.disabled
            - audit.enabled
            - augur.access_denied
            - augur.fetch_brokered
            - bootstrap.delivered
            - cadence.created
            - cadence.deleted
            - cadence.skipped_forbidden
            - cadence.skipped_overlap
            - cadence.spawned
            - cadence.updated
            - cert.issued
            - cert.registered
            - cert.rotated
            - choir.created
            - choir.deleted
            - choir.voice_added
            - choir.voice_removed
            - cloud.provisioned
            - cluster.degraded_cleared
            - cluster.degraded_set
            - command_run.cancelled
            - command_run.completed
            - command_run.failed
            - command_run.invoked
            - command_run.partial_failed
            - config.reload_failed
            - config.reload_succeeded
            - console.closed
            - console.command
            - console.opened
            - console.recording-read
            - decree.circuit_tripped
            - decree.created
            - decree.deleted
            - errand.cancelled
            - errand.completed
            - errand.failed
            - errand.invoked
            - errand.timed_out
            - eventstream.lease_force_released
            - herald.created
            - herald.deleted
            - herald.delivered
            - herald.failed
            - herald.updated
            - incarnation.created
            - incarnation.destroy_completed
            - incarnation.destroy_failed
            - incarnation.destroy_started
            - incarnation.drift_checked
            - incarnation.member_bound
            - incarnation.member_unbound
            - incarnation.rerun_last
            - incarnation.run_completed
            - incarnation.scenario_started
            - incarnation.secret_revealed
            - incarnation.traits_changed
            - incarnation.unlocked
            - incarnation.upgrade_started
            - input.vault_resolved
            - omen.created
            - omen.revoked
            - operator.created
            - operator.login
            - operator.provisioned
            - operator.revoked
            - operator.token-issued
            - oracle.fired
            - plugin.allowed
            - plugin.revoked
            - profile.created
            - profile.deleted
            - provider.created
            - provider.deleted
            - provisioning.policy_changed
            - push-provider.created
            - push-provider.deleted
            - push-provider.imported_from_config
            - push-provider.updated
            - push.applied
            - push.completed
            - push.failed
            - push.partial_failed
            - reaper.reconcile_orphan_applying.executed
            - rite.created
            - rite.revoked
            - role.created
            - role.deleted
            - role.operator-granted
            - role.operator-revoked
            - role.permissions-updated
            - run.completed
            - scenario_run.cancelled
            - scenario_run.completed
            - scenario_run.failed
            - scenario_run.lease_lost
            - scenario_run.leg_completed
            - scenario_run.leg_started
            - scenario_run.partial_failed
            - scenario_run.started
            - service.deregistered
            - service.registered
            - service.updated
            - setting.deleted
            - setting.updated
            - sigil.key-introduced
            - sigil.key-primary-set
            - sigil.key-retired
            - soul.bootstrapped
            - soul.coven-changed
            - soul.created
            - soul.seed-issued
            - soul.seed-rotated
            - soul.ssh-target.imported_from_config
            - soul.ssh-target.updated
            - soul.token-issued
            - soul.traits-changed
            - soulprint.received
            - synod.created
            - synod.deleted
            - synod.operator-added
            - synod.operator-removed
            - synod.role-granted
            - synod.role-revoked
            - synod.updated
            - task.executed
            - tiding.created
            - tiding.deleted
            - tiding.updated
            - vault.kv-present
            - vault.kv-read
            - vigil.created
            - vigil.deleted
            - voyage.reclaimed
          type: string
      required:
        - created_at
        - id
        - payload
        - source
        - type
      type: object
    AuditEventListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/AuditEvent"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    AuthMethodsReply:
      additionalProperties: false
      properties:
        ldap:
          description: federated LDAP login is configured
          type: boolean
        oidc:
          description: federated OIDC login is configured
          type: boolean
        password:
          description: local login (always available)
          type: boolean
      required:
        - password
        - ldap
        - oidc
      type: object
    AuthTokenReply:
      additionalProperties: false
      properties:
        expires_at:
          description: expiration moment of the issued Bearer
          format: date-time
          type: string
        token:
          description: short Bearer JWT for Authorization on /v1
          type: string
      required:
        - token
        - expires_at
      type: object
    Cadence:
      additionalProperties: false
      properties:
        batch_mode:
          enum:
            - barrier
            - window
          type: string
        batch_percent:
          format: int64
          type: integer
        batch_size:
          format: int64
          type: integer
        cadence_id:
          description: ULID of schedule
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        concurrency:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        cron_expr:
          type: string
        enabled:
          type: boolean
        fail_threshold:
          format: int64
          type: integer
        fail_threshold_percent:
          format: int64
          type: integer
        interval_seconds:
          format: int64
          type: integer
        kind:
          enum:
            - scenario
            - command
          type: string
        last_run_at:
          format: date-time
          type: string
        module:
          type: string
        name:
          type: string
        next_run_at:
          format: date-time
          type: string
        on_failure:
          enum:
            - abort
            - continue
          type: string
        overlap_policy:
          enum:
            - skip
            - queue
            - parallel
          type: string
        require_alive:
          type: boolean
        scenario_name:
          type: string
        schedule_kind:
          enum:
            - interval
            - cron
          type: string
        target:
          $ref: "#/components/schemas/VoyageTarget"
          description: declarative run target (declarative, passed through as-is)
        updated_at:
          format: date-time
          type: string
      required:
        - cadence_id
        - name
        - enabled
        - schedule_kind
        - overlap_policy
        - kind
        - created_by_aid
        - created_at
        - updated_at
      type: object
    CadenceCreateReply:
      additionalProperties: false
      properties:
        cadence_id:
          description: ULID of created schedule
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        enabled:
          type: boolean
        location:
          description: relative resource URL
          type: string
        name:
          type: string
        next_run_at:
          description: RFC3339 time of next run
          format: date-time
          type: string
      required:
        - cadence_id
        - name
        - enabled
        - location
      type: object
    CadenceCreateRequest:
      additionalProperties: false
      properties:
        batch:
          description: "batch size: N hosts/incarnations or N%"
          type: string
        batch_mode:
          type: string
        batch_percent:
          format: int64
          maximum: 100
          minimum: 1
          type: integer
        batch_size:
          format: int64
          minimum: 1
          type: integer
        concurrency:
          format: int64
          minimum: 1
          type: integer
        cron_expr:
          description: cron expression for schedule_kind=cron
          type: string
        enabled:
          description: enable/disable scheduler (default true)
          type: boolean
        fail_threshold:
          format: int64
          minimum: 1
          type: integer
        input:
          additionalProperties: {}
          description: recipe parameters
          type: object
        inter_batch_interval_ms:
          format: int64
          type: integer
        inter_unit_interval_ms:
          format: int64
          type: integer
        interval_seconds:
          description: period for schedule_kind=interval (minimum 30s — absolute poll_floor, ADR-046/048)
          format: int64
          minimum: 30
          type: integer
        kind:
          description: run recipe type
          enum:
            - scenario
            - command
          type: string
        max_failures:
          description: "failure threshold: N absolute or N%"
          type: string
        module:
          description: module for kind=command
          type: string
        name:
          description: human-readable schedule name
          type: string
        notify:
          description: subscriptions to run notifications for this schedule
          items:
            $ref: "#/components/schemas/VoyageNotify"
          type:
            - array
            - "null"
        on_failure:
          type: string
        overlap_policy:
          description: overlap policy for runs
          enum:
            - skip
            - queue
            - parallel
          type: string
        require_alive:
          type: boolean
        scenario_name:
          description: scenario name for kind=scenario
          type: string
        schedule_kind:
          description: schedule type
          enum:
            - interval
            - cron
          type: string
        target:
          $ref: "#/components/schemas/VoyageTarget"
          description: run target (resolved on spawn)
      required:
        - name
        - schedule_kind
        - overlap_policy
        - kind
        - target
      type: object
    CadenceEnabledReply:
      additionalProperties: false
      properties:
        cadence_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        enabled:
          type: boolean
      required:
        - cadence_id
        - enabled
      type: object
    CadenceListReply:
      additionalProperties: false
      properties:
        items:
          description: page of schedules
          items:
            $ref: "#/components/schemas/Cadence"
          type:
            - array
            - "null"
        limit:
          description: page size
          format: int32
          type: integer
        offset:
          description: offset from start of set
          format: int32
          type: integer
        total:
          description: total number of entries in set
          format: int32
          type: integer
      required:
        - items
        - offset
        - limit
        - total
      type: object
    CadencePatchRequest:
      additionalProperties: false
      properties:
        batch:
          description: "batch size: N hosts/incarnations or N%"
          type: string
        batch_mode:
          type: string
        batch_percent:
          format: int64
          maximum: 100
          minimum: 1
          type: integer
        batch_size:
          format: int64
          minimum: 1
          type: integer
        concurrency:
          format: int64
          minimum: 1
          type: integer
        cron_expr:
          description: cron expression (empty string → clear)
          type: string
        enabled:
          description: enable/disable scheduler
          type: boolean
        fail_threshold:
          format: int64
          minimum: 1
          type: integer
        input:
          additionalProperties: {}
          description: recipe parameters
          type: object
        interval_seconds:
          description: period for schedule_kind=interval (minimum 30s — absolute poll_floor, ADR-046/048)
          format: int64
          minimum: 30
          type: integer
        max_failures:
          description: "failure threshold: N absolute or N%"
          type: string
        module:
          description: module for kind=command (empty string → clear)
          type: string
        name:
          description: human-readable schedule name
          type: string
        on_failure:
          description: abort|continue (empty string → clear)
          type: string
        overlap_policy:
          description: overlap policy for runs
          enum:
            - skip
            - queue
            - parallel
          type: string
        require_alive:
          type: boolean
        scenario_name:
          description: scenario name (empty string → clear)
          type: string
        schedule_kind:
          description: schedule type
          enum:
            - interval
            - cron
          type: string
        target:
          $ref: "#/components/schemas/VoyageTarget"
          description: run target
      type: object
    Choir:
      additionalProperties: false
      properties:
        choir_name:
          type: string
        created_at:
          format: date-time
          type: string
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type:
            - string
            - "null"
        description:
          type:
            - string
            - "null"
        incarnation_name:
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        max_size:
          format: int64
          type:
            - integer
            - "null"
        min_size:
          format: int64
          type:
            - integer
            - "null"
      required:
        - choir_name
        - created_at
        - created_by_aid
        - description
        - incarnation_name
        - max_size
        - min_size
      type: object
    ChoirCreateRequest:
      additionalProperties: false
      properties:
        choir_name:
          description: Choir name (^[a-z][a-z0-9_-]*$)
          pattern: ^[a-z][a-z0-9_-]*$
          type: string
        description:
          description: human-readable description
          type: string
        max_size:
          description: upper batch size bound (>= min_size)
          format: int64
          type: integer
        min_size:
          description: lower batch size bound (> 0)
          format: int64
          type: integer
      required:
        - choir_name
      type: object
    ChoirListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/Choir"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    ClusterInstanceEntry:
      additionalProperties: false
      properties:
        alive:
          type: boolean
        is_reaper_leader:
          type: boolean
        kid:
          type: string
        started_at:
          format: date-time
          type: string
      required:
        - kid
        - alive
        - is_reaper_leader
      type: object
    ClusterReply:
      additionalProperties: false
      properties:
        instances:
          items:
            $ref: "#/components/schemas/ClusterInstanceEntry"
          type:
            - array
            - "null"
        self_health:
          additionalProperties:
            type: string
          type: object
        self_kid:
          type: string
      required:
        - instances
        - self_kid
        - self_health
      type: object
    CompatEntityView:
      additionalProperties: false
      properties:
        compatible:
          type: boolean
        kind:
          type: string
        name:
          type: string
        ref:
          type: string
        window:
          $ref: "#/components/schemas/CompatWindowView"
      required:
        - kind
        - name
        - window
        - compatible
      type: object
    CompatWindowView:
      additionalProperties: false
      properties:
        display:
          type: string
        max:
          type: string
        min:
          type: string
      required:
        - display
      type: object
    ConsoleRecording:
      additionalProperties: false
      properties:
        archon_aid:
          description: the Archon who held the session
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        byte_count:
          description: encoded size of the cast body in bytes
          format: int64
          type: integer
        close_reason:
          type: string
        event_count:
          format: int64
          type: integer
        finished_at:
          description: absent while the session is live, and also when the Keeper instance holding it died mid-recording — the body up to that point is complete and replayable
          format: date-time
          type: string
        height:
          description: terminal rows from the cast header
          format: int32
          minimum: 0
          type: integer
        kind:
          description: interactive = a PTY session over GET /v1/console; command = a one-shot keeper.soul.run-command
          enum:
            - interactive
            - command
          type: string
        recording_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        session_id:
          type: string
        sid:
          description: the host the session ran on
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
        started_at:
          format: date-time
          type: string
        truncated:
          description: the session hit the per-session recording cap and was closed; the recording ends before the shell did
          type: boolean
        width:
          description: terminal columns from the cast header
          format: int32
          minimum: 0
          type: integer
      required:
        - archon_aid
        - byte_count
        - event_count
        - height
        - kind
        - recording_id
        - session_id
        - sid
        - started_at
        - truncated
        - width
      type: object
    ConsoleRecordingListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ConsoleRecording"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - offset
        - limit
        - total
      type: object
    DecreeCreateRequest:
      additionalProperties: false
      properties:
        action_input:
          description: scenario input (vault-ref passed through as-is)
        action_scenario:
          description: named scenario (whitelist; raw command rejected)
          pattern: ^[a-z][a-z0-9_]*$
          type: string
        cooldown:
          description: minimum interval between triggers per-(decree, subject)
          type: string
        enabled:
          description: whether the rule is active (default true)
          type: boolean
        incarnation_name:
          description: target incarnation of the reaction (required)
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        name:
          description: Decree name (kebab-case, 1..63)
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        on_beacon:
          description: Vigil name whose Portent the rule reacts to
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        subject:
          $ref: "#/components/schemas/Subject"
          description: which hosts may fire the rule — exactly one of sid / incarnation / coven / trait
        where:
          description: opt. CEL predicate over event.data; compile-checked
          type: string
      required:
        - name
        - on_beacon
        - subject
        - incarnation_name
        - action_scenario
      type: object
    DecreeListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/DecreeView"
          type:
            - array
            - "null"
        limit:
          format: int32
          type: integer
        offset:
          format: int32
          type: integer
        total:
          format: int32
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    DecreeView:
      additionalProperties: false
      properties:
        action_input: {}
        action_scenario:
          type: string
        cooldown:
          type: string
        created_at:
          format: date-time
          type: string
        created_by_aid:
          type: string
        enabled:
          type: boolean
        incarnation_name:
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        name:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        on_beacon:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        subject:
          $ref: "#/components/schemas/Subject"
        updated_at:
          format: date-time
          type: string
        where:
          type: string
      required:
        - action_input
        - action_scenario
        - cooldown
        - created_at
        - enabled
        - incarnation_name
        - name
        - on_beacon
        - subject
        - updated_at
      type: object
    DeprecationEntry:
      additionalProperties: false
      properties:
        module:
          type: string
        param:
          type: string
        removed_in:
          type: string
        since:
          type: string
        sites:
          items:
            $ref: "#/components/schemas/DeprecationSiteEntry"
          type:
            - array
            - "null"
        use:
          type: string
      required:
        - module
        - param
        - sites
      type: object
    DeprecationGapEntry:
      additionalProperties: false
      properties:
        detail:
          type: string
        incarnations:
          items:
            type: string
          type:
            - array
            - "null"
        reason:
          type: string
        scope:
          enum:
            - service
            - module
          type: string
        subject:
          type: string
      required:
        - scope
        - subject
        - reason
        - incarnations
      type: object
    DeprecationSiteEntry:
      additionalProperties: false
      properties:
        incarnation:
          type: string
        scenario:
          type: string
        service:
          type: string
        service_version:
          type: string
        where:
          type: string
      required:
        - incarnation
        - service
        - scenario
      type: object
    DeprecationsReply:
      additionalProperties: false
      properties:
        gaps:
          items:
            $ref: "#/components/schemas/DeprecationGapEntry"
          type:
            - array
            - "null"
        items:
          items:
            $ref: "#/components/schemas/DeprecationEntry"
          type:
            - array
            - "null"
        scanned_definitions:
          format: int64
          type: integer
        scanned_incarnations:
          format: int64
          type: integer
        truncated:
          type: boolean
      required:
        - items
        - gaps
        - scanned_incarnations
        - scanned_definitions
      type: object
    DriftHostReport:
      additionalProperties: false
      properties:
        sid:
          type: string
        status:
          type: string
        tasks:
          items:
            $ref: "#/components/schemas/DriftTaskResult"
          type:
            - array
            - "null"
      required:
        - sid
        - status
        - tasks
      type: object
    DriftReport:
      additionalProperties: false
      properties:
        checked_at:
          format: date-time
          type: string
        hosts:
          items:
            $ref: "#/components/schemas/DriftHostReport"
          type:
            - array
            - "null"
        incarnation:
          type: string
        scenario_ref:
          type: string
        summary:
          $ref: "#/components/schemas/DriftSummary"
      required:
        - checked_at
        - incarnation
        - scenario_ref
        - hosts
        - summary
      type: object
    DriftScanSummary:
      additionalProperties: false
      properties:
        hosts_clean:
          format: int64
          type: integer
        hosts_drifted:
          format: int64
          type: integer
        hosts_failed:
          format: int64
          type: integer
        hosts_unsupported:
          format: int64
          type: integer
        scanned_at:
          format: date-time
          type: string
        total_hosts:
          format: int64
          type: integer
      required:
        - hosts_clean
        - hosts_drifted
        - hosts_failed
        - hosts_unsupported
        - scanned_at
        - total_hosts
      type: object
    DriftSummary:
      additionalProperties: false
      properties:
        hosts_clean:
          format: int64
          type: integer
        hosts_drifted:
          format: int64
          type: integer
        hosts_failed:
          format: int64
          type: integer
        hosts_unsupported:
          format: int64
          type: integer
      required:
        - hosts_drifted
        - hosts_clean
        - hosts_unsupported
        - hosts_failed
      type: object
    DriftTaskResult:
      additionalProperties: false
      properties:
        action:
          type: string
        changed:
          type: boolean
        idx:
          format: int64
          type: integer
        message:
          type: string
        module:
          type: string
      required:
        - idx
        - module
        - changed
      type: object
    ErrandAccepted:
      additionalProperties: false
      properties:
        errand_id:
          description: ULID of the started Errand
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        status:
          description: string still running (async-escalation)
          enum:
            - running
          type: string
      required:
        - errand_id
        - status
      type: object
    ErrandListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ErrandResult"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    ErrandResult:
      additionalProperties: false
      properties:
        duration_ms:
          format: int64
          type: integer
        errand_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        error_message:
          type: string
        exit_code:
          format: int32
          type: integer
        finished_at:
          format: date-time
          type: string
        module:
          type: string
        output:
          additionalProperties: {}
          type: object
        sid:
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
        started_at:
          format: date-time
          type: string
        started_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        status:
          type: string
        stderr:
          type: string
        stderr_truncated:
          type: boolean
        stdout:
          type: string
        stdout_truncated:
          type: boolean
      required:
        - errand_id
        - module
        - sid
        - started_at
        - started_by_aid
        - status
      type: object
    ErrandRunRequest:
      additionalProperties: false
      properties:
        dry_run:
          description: only for PlanReadSafe modules; verb module (shell/exec) -> 400
          type: boolean
        input:
          additionalProperties: {}
          description: input for the module (validated against input_schema)
          type: object
        module:
          description: fully-qualified <ns>.<name>.<state> (core.cmd.shell / core.exec.run / ErrandReadSafe module)
          type: string
        timeout_seconds:
          description: total Errand timeout [1..300]; 0/omitted -> default 30s; > server-cap (30s) -> 202 + Location
          format: int64
          maximum: 300
          type: integer
      required:
        - module
      type: object
    EventTypeArea:
      additionalProperties: false
      properties:
        name:
          type: string
      required:
        - name
      type: object
    EventTypeCatalogReply:
      additionalProperties: false
      properties:
        areas:
          items:
            $ref: "#/components/schemas/EventTypeArea"
          type:
            - array
            - "null"
        point_events:
          items:
            $ref: "#/components/schemas/EventTypePoint"
          type:
            - array
            - "null"
      required:
        - areas
        - point_events
      type: object
    EventTypePoint:
      additionalProperties: false
      properties:
        name:
          type: string
      required:
        - name
      type: object
    GitRef:
      additionalProperties: false
      properties:
        commit:
          type: string
        is_default:
          type: boolean
        name:
          type: string
        type:
          type: string
      required:
        - commit
        - name
        - type
      type: object
    GlobalRunEntry:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        finished_at:
          format: date-time
          type: string
        incarnation:
          type: string
        scenario:
          type: string
        service:
          type: string
        started_at:
          format: date-time
          type: string
        started_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        status:
          enum:
            - applying
            - success
            - failed
            - cancelled
          type: string
      required:
        - apply_id
        - incarnation
        - service
        - scenario
        - status
        - started_at
      type: object
    GrantOperatorRequest:
      additionalProperties: false
      properties:
        aid:
          description: AID of the archon being assigned to a role/group (naming-rules.md)
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
      required:
        - aid
      type: object
    Herald:
      additionalProperties: false
      properties:
        config:
          additionalProperties: {}
          type: object
        created_at:
          format: date-time
          type: string
        created_by_aid:
          type: string
        enabled:
          type: boolean
        name:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        secret_ref:
          type: string
        type:
          type: string
        updated_at:
          format: date-time
          type: string
      required:
        - config
        - created_at
        - enabled
        - name
        - type
        - updated_at
      type: object
    HeraldCreateRequest:
      additionalProperties: false
      properties:
        config:
          additionalProperties: {}
          description: "per-type config (form depends on type; see catalog GET /v1/herald-types). Channel secret (bot_token/webhook_url/header_secret) — dual-mode: value (plaintext) OR *_ref (vault path)"
          type: object
        enabled:
          description: channel enabled (omitted → true)
          type: boolean
        name:
          description: Herald channel name (kebab-case, 1..63), unique in the cluster
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        secret:
          description: "opt. plaintext webhook signing-token (dual-mode, ADR-064): keeper writes it into Vault itself; XOR with secret_ref. Requires TLS-front (secret_ingest.accept_plaintext)"
          type: string
        secret_ref:
          description: opt. vault-ref on webhook signing-token (vault:<mount>/<path>); XOR with secret
          type: string
        type:
          description: "channel type (closed-enum: webhook|telegram|slack|mattermost|discord|custom|email); value outside the enum -> 422"
          enum:
            - custom
            - discord
            - email
            - mattermost
            - slack
            - telegram
            - webhook
          type: string
      required:
        - name
        - type
        - config
      type: object
    HeraldListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/Herald"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    HeraldTypeCatalogEntry:
      additionalProperties: false
      properties:
        fields:
          items:
            $ref: "#/components/schemas/HeraldTypeFieldSpec"
          type:
            - array
            - "null"
        secret_required:
          type: boolean
        type:
          type: string
      required:
        - type
        - fields
        - secret_required
      type: object
    HeraldTypeCatalogReply:
      additionalProperties: false
      properties:
        types:
          items:
            $ref: "#/components/schemas/HeraldTypeCatalogEntry"
          type:
            - array
            - "null"
      required:
        - types
      type: object
    HeraldTypeFieldSpec:
      additionalProperties: false
      properties:
        enum_values:
          items:
            type: string
          type:
            - array
            - "null"
        kind:
          type: string
        label:
          type: string
        name:
          type: string
        required:
          type: boolean
        secret:
          type: boolean
      required:
        - name
        - label
        - required
        - secret
        - kind
      type: object
    HeraldUpdateRequest:
      additionalProperties: false
      properties:
        config:
          additionalProperties: {}
          description: "per-type config (replace — fully replaces the existing one). Channel secret — dual-mode: value (plaintext) OR *_ref"
          type: object
        enabled:
          description: channel enabled (omitted → true)
          type: boolean
        secret:
          description: "opt. plaintext webhook signing-token (dual-mode, ADR-064): keeper overwrites it in Vault at the same path; XOR with secret_ref"
          type: string
        secret_ref:
          description: opt. vault-ref on signing-token; XOR with secret; absence of both clears the signature
          type: string
        type:
          description: "channel type (closed-enum: webhook|telegram|slack|mattermost|discord|custom|email)"
          enum:
            - custom
            - discord
            - email
            - mattermost
            - slack
            - telegram
            - webhook
          type: string
      required:
        - type
        - config
      type: object
    HostTelemetry:
      additionalProperties: false
      properties:
        collected_at:
          format: date-time
          type: string
        latest:
          $ref: "#/components/schemas/UtilizationLatest"
        sid:
          type: string
        stale:
          type: boolean
      required:
        - sid
        - stale
      type: object
    HumaProblemError:
      additionalProperties: false
      properties:
        detail:
          type: string
        instance:
          type: string
        status:
          format: int64
          type: integer
        title:
          type: string
        type:
          type: string
      required:
        - type
        - title
        - status
      type: object
    IncarnationCheckDriftRequest:
      additionalProperties: false
      properties:
        input:
          additionalProperties: {}
          description: override converge parameters (ADR-031 Slice B)
          type: object
      type: object
    IncarnationCreateReply:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        incarnation:
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
      required:
        - incarnation
      type: object
    IncarnationCreateRequest:
      additionalProperties: false
      properties:
        covens:
          description: declared environment tags (ADR-008 amendment a)
          items:
            maxLength: 63
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        create_scenario:
          description: "name of start scenario (mechanism for multiple creates, scenario with create:true). Empty: service offers create scenarios → 422 create_scenario_required; service without them → bare incarnation (ready without run)"
          pattern: ^[a-z][a-z0-9_]*$
          type: string
        input:
          additionalProperties: {}
          description: input for selected create scenario
          type: object
        name:
          description: new instance name (kebab-case); omit when the create scenario declares name_template (ADR-0079) — then it is composed server-side from input components
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        service:
          description: service name from registry (ADR-029)
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        traits:
          additionalProperties: {}
          description: operator-set trait labels (key → scalar|list of scalars), ADR-060
          type: object
      required:
        - service
      type: object
    IncarnationDestroyReply:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - apply_id
      type: object
    IncarnationFormPrefillReply:
      additionalProperties: false
      properties:
        values:
          additionalProperties: {}
          description: field → current value from incarnation.state (prefill-hint)
          type: object
      required:
        - values
      type: object
    IncarnationGetReply:
      additionalProperties: false
      properties:
        applying_apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        covens:
          items:
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        created_at:
          format: date-time
          type: string
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type:
            - string
            - "null"
        created_scenario:
          type: string
        last_drift_check_at:
          format: date-time
          type: string
        last_drift_summary:
          $ref: "#/components/schemas/DriftScanSummary"
        name:
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        service:
          type: string
        service_version:
          type: string
        state:
          additionalProperties: {}
          type: object
        state_schema_version:
          format: int32
          type: integer
        status:
          $ref: "#/components/schemas/IncarnationStatus"
        status_details:
          additionalProperties: {}
          type: object
        traits:
          additionalProperties: {}
          type: object
        updated_at:
          format: date-time
          type: string
      required:
        - covens
        - created_at
        - created_by_aid
        - name
        - service
        - service_version
        - state
        - state_schema_version
        - status
        - status_details
        - updated_at
      type: object
    IncarnationHistoryReply:
      additionalProperties: false
      properties:
        items:
          description: page of state_history entries
          items:
            $ref: "#/components/schemas/StateHistoryEntry"
          type:
            - array
            - "null"
        limit:
          description: page size
          format: int32
          type: integer
        offset:
          description: offset from start of set
          format: int32
          type: integer
        total:
          description: total number of entries in set
          format: int32
          type: integer
      required:
        - items
        - offset
        - limit
        - total
      type: object
    IncarnationListReply:
      additionalProperties: false
      properties:
        items:
          description: page of incarnations
          items:
            $ref: "#/components/schemas/IncarnationGetReply"
          type:
            - array
            - "null"
        limit:
          description: page size
          format: int32
          type: integer
        offset:
          description: offset from start of set
          format: int32
          type: integer
        total:
          description: total number of entries in set
          format: int32
          type: integer
      required:
        - items
        - offset
        - limit
        - total
      type: object
    IncarnationMember:
      additionalProperties: false
      properties:
        bound_at:
          format: date-time
          type: string
        bound_by_aid:
          type: string
        sid:
          type: string
        status:
          type: string
      required:
        - sid
        - status
        - bound_at
      type: object
    IncarnationMemberBindReply:
      additionalProperties: false
      properties:
        already_member:
          items:
            type: string
          type:
            - array
            - "null"
        bound:
          items:
            type: string
          type:
            - array
            - "null"
        incarnation:
          type: string
      required:
        - incarnation
        - bound
        - already_member
      type: object
    IncarnationMemberBindRequest:
      additionalProperties: false
      properties:
        sids:
          description: SIDs (FQDN) of already-onboarded, connected hosts to bind to this incarnation
          items:
            pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
            type: string
          maxItems: 200
          minItems: 1
          type:
            - array
            - "null"
      required:
        - sids
      type: object
    IncarnationMemberListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/IncarnationMember"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    IncarnationRerunLastReply:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        incarnation:
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        scenario:
          pattern: ^[a-z][a-z0-9_]*$
          type: string
      required:
        - apply_id
        - incarnation
        - scenario
      type: object
    IncarnationRerunLastRequest:
      additionalProperties: false
      properties:
        input:
          additionalProperties: {}
          description: operator input, accepted only when the attempt cannot be replayed from history
          type: object
        reason:
          description: free text confirmation
          maxLength: 500
          minLength: 1
          type: string
      required:
        - reason
      type: object
    IncarnationResolveNameReply:
      additionalProperties: false
      properties:
        available:
          description: no incarnation holds this name (meaningful only when valid)
          type: boolean
        composed_name:
          description: the name a create with this input would produce; carries the offending value when invalid
          type: string
        composes:
          description: the chosen create scenario composes the name from name_template (ADR-0079); false → the operator names the incarnation
          type: boolean
        invalid_reason:
          description: why the name could not be composed or was rejected, in operator terms
          type: string
        length:
          description: character count of composed_name
          format: int64
          type: integer
        max_length:
          description: the incarnation name ceiling — server-sourced so the form does not restate it
          format: int64
          type: integer
        taken_by_service:
          description: service of the incarnation holding the name — only when the caller may see it
          type: string
        valid:
          description: composed_name is a legal incarnation name
          type: boolean
      required:
        - composes
        - composed_name
        - length
        - max_length
        - valid
        - available
      type: object
    IncarnationResolveNameRequest:
      additionalProperties: false
      properties:
        covens:
          description: declared environment tags of the intended create — scope parity with POST /v1/incarnations, not part of the composition
          items:
            maxLength: 63
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        create_scenario:
          description: chosen create scenario, whose name_template composes the name
          pattern: ^[a-z][a-z0-9_]*$
          type: string
        input:
          additionalProperties: {}
          description: the create input so far — partial is expected, this is a live preview
          type: object
        service:
          description: service name from registry (ADR-029)
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
      required:
        - service
      type: object
    IncarnationRevealSecretReply:
      additionalProperties: false
      properties:
        value:
          description: plaintext value of the secret
          type: string
      required:
        - value
      type: object
    IncarnationRevealSecretRequest:
      additionalProperties: false
      properties:
        key:
          description: element key of the current-state enumerate array (element.name)
          type: string
        secret_id:
          description: id of the revealable secret (revealable_secrets of the service manifest)
          type: string
      required:
        - secret_id
        - key
      type: object
    IncarnationRevealableSecretItem:
      additionalProperties: false
      properties:
        keys:
          description: allowed keys (element.name of the current state)
          items:
            type: string
          type:
            - array
            - "null"
        label:
          description: label for UI
          type: string
        secret_id:
          description: id of the secret (passed as secret_id on reveal)
          type: string
        state_path:
          description: state path of the array (tail enumerate, e.g. redis_users)
          type: string
      required:
        - secret_id
        - label
        - state_path
        - keys
      type: object
    IncarnationRevealableSecretsReply:
      additionalProperties: false
      properties:
        items:
          description: revealable secrets of the incarnation
          items:
            $ref: "#/components/schemas/IncarnationRevealableSecretItem"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    IncarnationRunReply:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        incarnation:
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        scenario:
          type: string
      required:
        - apply_id
        - incarnation
        - scenario
      type: object
    IncarnationRunRequest:
      additionalProperties: false
      properties:
        input:
          additionalProperties: {}
          description: scenario input
          type: object
        name:
          description: echo path-name (ignored)
          type: string
        scenario:
          description: echo path-scenario (ignored)
          type: string
      type: object
    IncarnationRunsReply:
      additionalProperties: false
      properties:
        items:
          description: page of incarnation runs (apply_runs fold)
          items:
            $ref: "#/components/schemas/RunSummaryEntry"
          type:
            - array
            - "null"
        limit:
          description: page size
          format: int32
          type: integer
        offset:
          description: offset from start of set
          format: int32
          type: integer
        total:
          description: total number of incarnation runs
          format: int32
          type: integer
      required:
        - items
        - offset
        - limit
        - total
      type: object
    IncarnationSetTraitsRequest:
      additionalProperties: false
      properties:
        traits:
          additionalProperties: {}
          description: full set of trait-tags (key -> scalar|list of scalars); empty/omitted = clear (ADR-060)
          type: object
      type: object
    IncarnationStatus:
      description: "Runtime instance status. In proto the constants have a family-prefix (INCARNATION_STATUS_READY), in the JSON API - short forms. `drift` - an informational Scry status (ADR-031), NOT blocking: remediation = a regular apply, which on success returns the incarnation to `ready`."
      enum:
        - provisioning
        - ready
        - applying
        - error_locked
        - migration_failed
        - drift
        - destroying
        - destroy_failed
      type: string
    IncarnationTelemetryReply:
      additionalProperties: false
      properties:
        hosts:
          items:
            $ref: "#/components/schemas/HostTelemetry"
          type:
            - array
            - "null"
        incarnation:
          type: string
        truncated:
          description: true if the incarnation's members exceeded the cap and the host list is truncated (overview glance)
          type: boolean
      required:
        - incarnation
        - truncated
        - hosts
      type: object
    IncarnationUnlockReply:
      additionalProperties: false
      properties:
        name:
          type: string
        previous_status:
          $ref: "#/components/schemas/IncarnationStatus"
        status:
          $ref: "#/components/schemas/IncarnationStatus"
        unlocked_at:
          format: date-time
          type: string
        unlocked_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
      required:
        - name
        - previous_status
        - status
        - unlocked_at
        - unlocked_by_aid
      type: object
    IncarnationUnlockRequest:
      additionalProperties: false
      properties:
        name:
          description: echo path-name (ignored)
          type: string
        reason:
          description: free text confirmation
          maxLength: 500
          minLength: 1
          type: string
      required:
        - reason
      type: object
    IncarnationUpgradePathsReply:
      additionalProperties: false
      properties:
        current_state_schema_version:
          format: int64
          type: integer
        current_version:
          type: string
        paths:
          items:
            $ref: "#/components/schemas/UpgradePathRef"
          type:
            - array
            - "null"
        target:
          $ref: "#/components/schemas/UpgradePathTarget"
      required:
        - current_version
        - current_state_schema_version
      type: object
    IncarnationUpgradeReply:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        run_apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - apply_id
      type: object
    IncarnationUpgradeRequest:
      additionalProperties: false
      properties:
        name:
          description: echo path-name (ignored)
          type: string
        to_version:
          description: target service version (git-ref)
          type: string
      required:
        - to_version
      type: object
    IssueTokenReply:
      additionalProperties: false
      properties:
        aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        expires_at:
          format: date-time
          type: string
        jwt:
          type: string
      required:
        - aid
        - expires_at
        - jwt
      type: object
    LDAPLoginRequest:
      additionalProperties: false
      properties:
        password:
          description: password (never logged, never returned)
          format: password
          minLength: 1
          type: string
        username:
          description: username for LDAP search-bind
          minLength: 1
          type: string
      required:
        - username
        - password
      type: object
    ModuleCatalogItem:
      additionalProperties: false
      properties:
        description:
          type: string
        errand_safe:
          type: boolean
        introduced_in:
          type: string
        kind:
          type: string
        name:
          type: string
        namespace:
          type: string
        params:
          items:
            $ref: "#/components/schemas/ModuleParam"
          type:
            - array
            - "null"
        states:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - name
        - kind
        - states
        - errand_safe
        - params
      type: object
    ModuleCatalogReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ModuleCatalogItem"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    ModuleDeprecation:
      additionalProperties: false
      properties:
        removed_in:
          type: string
        since:
          type: string
        use:
          type: string
      type: object
    ModuleFormPrepChoirSource:
      additionalProperties: false
      properties:
        incarnation:
          description: incarnation name of the Choir
          type: string
        name:
          description: Choir name
          type: string
      required:
        - incarnation
        - name
      type: object
    ModuleFormPrepReply:
      additionalProperties: false
      properties:
        sids:
          items:
            type: string
          type:
            - array
            - "null"
        truncated:
          type: boolean
      required:
        - sids
        - truncated
      type: object
    ModuleFormPrepRequest:
      additionalProperties: false
      properties:
        prefix:
          description: SID prefix for autocomplete (LIKE prefix%)
          type: string
        source:
          $ref: "#/components/schemas/ModuleFormPrepSource"
          description: source-catalog discriminator (exactly one of incarnation_hosts/choir)
      required:
        - source
      type: object
    ModuleFormPrepSource:
      additionalProperties: false
      properties:
        choir:
          $ref: "#/components/schemas/ModuleFormPrepChoirSource"
          description: coordinates of the Choir source (incarnation + Choir name)
        incarnation_hosts:
          description: incarnation name — live SIDs of its hosts
          type: string
      type: object
    ModuleInputSource:
      additionalProperties: false
      properties:
        choir:
          type: string
        incarnation_hosts:
          type: boolean
      type: object
    ModuleParam:
      additionalProperties: false
      properties:
        deprecated:
          $ref: "#/components/schemas/ModuleDeprecation"
        description:
          type: string
        enum:
          items: {}
          type:
            - array
            - "null"
        example:
          type: string
        format:
          type: string
        introduced_in:
          type: string
        items:
          $ref: "#/components/schemas/ModuleParam"
        multiline:
          type: boolean
        name:
          type: string
        pattern:
          type: string
        required:
          type: boolean
        secret:
          type: boolean
        source:
          $ref: "#/components/schemas/ModuleInputSource"
        type:
          type: string
      required:
        - name
        - required
      type: object
    MyPermission:
      additionalProperties: false
      properties:
        action:
          type: string
        resource:
          type: string
        scope:
          $ref: "#/components/schemas/MyPermissionScope"
        wildcard:
          type: boolean
      type: object
    MyPermissionScope:
      additionalProperties: false
      properties:
        exprs:
          items:
            type: string
          type: array
        unrestricted:
          type: boolean
      required:
        - unrestricted
      type: object
    MyPermissionsReply:
      additionalProperties: false
      properties:
        permissions:
          items:
            $ref: "#/components/schemas/MyPermission"
          type:
            - array
            - "null"
      required:
        - permissions
      type: object
    OmenCreateRequest:
      additionalProperties: false
      properties:
        auth_ref:
          description: vault-ref on master-credential (vault:<mount>/<path>); the secret itself is not stored
          type: string
        endpoint:
          description: external system URL (not a secret)
          type: string
        name:
          description: Omen name (kebab-case, 1..63)
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        source_type:
          description: external system type; a value outside the enum -> 422
          enum:
            - vault
            - prometheus
            - elk
          type: string
      required:
        - name
        - source_type
        - endpoint
        - auth_ref
      type: object
    OmenListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/OmenView"
          type:
            - array
            - "null"
        limit:
          format: int32
          type: integer
        offset:
          format: int32
          type: integer
        total:
          format: int32
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    OmenView:
      additionalProperties: false
      properties:
        auth_ref:
          type: string
        created_at:
          format: date-time
          type: string
        created_by_aid:
          type: string
        endpoint:
          type: string
        name:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        source_type:
          type: string
      required:
        - auth_ref
        - created_at
        - endpoint
        - name
        - source_type
      type: object
    Operator:
      additionalProperties: false
      properties:
        aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        auth_method:
          type: string
        bootstrap_initial:
          type: boolean
        created_at:
          format: date-time
          type: string
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        created_via:
          enum:
            - bootstrap
            - user
            - ldap
            - oidc
            - system
          type: string
        display_name:
          type: string
        metadata:
          additionalProperties: {}
          type: object
        revoked_at:
          format: date-time
          type: string
      required:
        - aid
        - auth_method
        - bootstrap_initial
        - created_at
        - created_via
        - display_name
      type: object
    OperatorCreateReply:
      additionalProperties: false
      properties:
        aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        created_at:
          format: date-time
          type: string
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        display_name:
          type: string
        jwt:
          type: string
        roles:
          items:
            type: string
          type: array
      required:
        - aid
        - created_at
        - created_by_aid
        - display_name
        - jwt
      type: object
    OperatorCreateRequest:
      additionalProperties: false
      properties:
        aid:
          description: AID of the new Archon (naming-rules.md)
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        display_name:
          description: human-readable name for UI/audit
          type: string
        roles:
          description: opt. list of roles for atomic create+grant (onboarding in one call); role error -> rollback
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - aid
      type: object
    OperatorListReply:
      additionalProperties: false
      properties:
        items:
          description: page of operators
          items:
            $ref: "#/components/schemas/Operator"
          type:
            - array
            - "null"
        limit:
          description: page size
          format: int32
          type: integer
        offset:
          description: offset from start of set
          format: int32
          type: integer
        total:
          description: total number of entries in set
          format: int32
          type: integer
      required:
        - items
        - offset
        - limit
        - total
      type: object
    OperatorRevokeRequest:
      additionalProperties: false
      properties:
        reason:
          description: free-text reason for audit-trail (optional)
          type: string
      type: object
    PermissionAction:
      additionalProperties: false
      properties:
        action:
          type: string
        selector_keys:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - action
        - selector_keys
      type: object
    PermissionCatalogItem:
      additionalProperties: false
      properties:
        actions:
          items:
            $ref: "#/components/schemas/PermissionAction"
          type:
            - array
            - "null"
        resource:
          type: string
      required:
        - actions
        - resource
      type: object
    PermissionCatalogReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/PermissionCatalogItem"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    PluginSigilAllowReply:
      additionalProperties: false
      properties:
        name:
          type: string
        namespace:
          type: string
        ref:
          type: string
        sha256:
          pattern: ^[0-9a-f]{64}$
          type: string
      required:
        - name
        - namespace
        - ref
        - sha256
      type: object
    PluginSigilAllowRequest:
      additionalProperties: false
      properties:
        name:
          description: plugin name (as in manifest.name)
          pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
          type: string
        namespace:
          description: plugin namespace (type — cloud/ssh/mod)
          pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
          type: string
        ref:
          description: git-tag-ref of the release (stable tag, no slashes)
          pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
          type: string
      required:
        - namespace
        - name
        - ref
      type: object
    PluginSigilListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/PluginSigilView"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    PluginSigilView:
      additionalProperties: false
      properties:
        allowed_at:
          format: date-time
          type: string
        allowed_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        name:
          type: string
        namespace:
          type: string
        ref:
          type: string
        revoked_at:
          format: date-time
          type: string
        sha256:
          pattern: ^[0-9a-f]{64}$
          type: string
      required:
        - allowed_at
        - allowed_by_aid
        - name
        - namespace
        - ref
        - sha256
      type: object
    ProvisioningPolicyReply:
      additionalProperties: false
      properties:
        allowed_methods:
          items:
            type: string
          type:
            - array
            - "null"
        policy_set:
          type: boolean
      required:
        - allowed_methods
        - policy_set
      type: object
    ProvisioningPolicyUpdateRequest:
      additionalProperties: false
      properties:
        allowed_methods:
          description: "allowed operator creation methods (anti-lockout: non-empty list from {user,ldap,oidc})"
          items:
            enum:
              - user
              - ldap
              - oidc
            type: string
          minItems: 1
          type:
            - array
            - "null"
      required:
        - allowed_methods
      type: object
    PushApplyReply:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - apply_id
      type: object
    PushApplyRequest:
      additionalProperties: false
      properties:
        cleanup_stale_versions:
          description: remove stale soul-binary/module versions in the same SSH session
          type: boolean
        destiny:
          description: reference to Destiny in the form <name>@<ref>
          type: string
        input:
          additionalProperties: {}
          description: input for destiny
          type: object
        inventory:
          description: "list of target SID (FQDN) hosts (transport: ssh)"
          items:
            type: string
          type:
            - array
            - "null"
        ssh_provider:
          description: SshProvider name; defaults to the first registered one
          type: string
      required:
        - inventory
        - destiny
      type: object
    PushApplyView:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        cleanup_stale:
          type: boolean
        destiny_ref:
          type: string
        finished_at:
          format: date-time
          type: string
        input:
          additionalProperties: {}
          type: object
        inventory_sids:
          items:
            type: string
          type:
            - array
            - "null"
        ssh_provider:
          type: string
        started_at:
          format: date-time
          type: string
        started_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        status:
          type: string
        summary:
          additionalProperties: {}
          type: object
      required:
        - apply_id
        - cleanup_stale
        - destiny_ref
        - inventory_sids
        - started_at
        - status
      type: object
    PushProvider:
      additionalProperties: false
      properties:
        created_at:
          format: date-time
          type: string
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        name:
          type: string
        params:
          additionalProperties: {}
          type: object
        updated_at:
          format: date-time
          type: string
        updated_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
      required:
        - created_at
        - created_by_aid
        - name
        - params
        - updated_at
      type: object
    PushProviderCreateRequest:
      additionalProperties: false
      properties:
        name:
          description: Push Provider name (= plugins.ssh_providers[].name)
          pattern: ^[a-z][a-z0-9-]{0,62}$
          type: string
        params:
          additionalProperties: {}
          description: opaque params; sensitive — vault-refs (values are not logged)
          type: object
      required:
        - name
      type: object
    PushProviderListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/PushProvider"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    PushProviderUpdateRequest:
      additionalProperties: false
      properties:
        params:
          additionalProperties: {}
          description: full new set of params (replace); sensitive — vault-refs
          type: object
      required:
        - params
      type: object
    PushRunListEntry:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        cleanup_stale:
          type: boolean
        destiny_ref:
          type: string
        finished_at:
          format: date-time
          type: string
        inventory_sids:
          items:
            type: string
          type:
            - array
            - "null"
        ssh_provider:
          type: string
        started_at:
          format: date-time
          type: string
        started_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        status:
          type: string
        summary_counts:
          $ref: "#/components/schemas/PushSummaryCounts"
      required:
        - apply_id
        - cleanup_stale
        - destiny_ref
        - inventory_sids
        - started_at
        - status
      type: object
    PushRunListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/PushRunListEntry"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    PushSummaryCounts:
      additionalProperties: false
      properties:
        fail_count:
          format: int64
          type: integer
        success_count:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      type: object
    RiteCreateRequest:
      additionalProperties: false
      properties:
        allow:
          description: allow-list; shape depends on Omen source_type (passed through as-is)
        delegate:
          description: false - broker (MVP-1); true - delegation (MVP-2)
          type: boolean
        omen:
          description: Omen the grant belongs to
          type: string
        subject:
          $ref: "#/components/schemas/Subject"
          description: which hosts the grant covers — exactly one of sid / incarnation / coven / trait
        token_num_uses:
          description: token use-count limit; vault-delegate only
          format: int64
          type: integer
        token_ttl:
          description: TTL of the minted scoped token; vault-delegate only
          type: string
      required:
        - omen
        - subject
        - allow
      type: object
    RiteListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/RiteView"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    RiteView:
      additionalProperties: false
      properties:
        allow: {}
        created_at:
          format: date-time
          type: string
        created_by_aid:
          type: string
        delegate:
          type: boolean
        id:
          format: int64
          type: integer
        omen:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        subject:
          $ref: "#/components/schemas/Subject"
        token_num_uses:
          format: int64
          type: integer
        token_ttl:
          type: string
      required:
        - allow
        - created_at
        - delegate
        - id
        - omen
        - subject
      type: object
    RoleCreateRequest:
      additionalProperties: false
      properties:
        default_scope:
          description: "role scope: boolean predicate over coven/service/incarnation/host/trait (e.g. coven in (a, b) AND host matches redis-*); omitted/null → role without scope. On a DERIVED role (parent_role set) this is the attenuating delta, conjoined with the parent's effective scope — write only the ADDED narrowing"
          type: string
        description:
          description: human-readable role description for UI/audit
          type: string
        name:
          description: role name (kebab-case), unique in cluster
          pattern: ^[a-z][a-z0-9-]*$
          type: string
        parent_role:
          description: "derive from this role (ADR-078): it becomes the ceiling and the new role can never exceed it — permissions outside it are refused, its scope is conjoined onto every permission. Omitted/null → a plain role. Requires the caller to hold the parent's rights"
          pattern: ^[a-z][a-z0-9-]*$
          type: string
        permissions:
          description: set of permission strings for role (e.g., incarnation.run, soul.*, *)
          items:
            type: string
          type:
            - array
            - "null"
        scope_mode:
          description: what default_scope means here (ADR-078). track (the default) keeps it a delta, so the parent's scope cascades in and moving the parent moves this role. pin materializes the parent's CURRENT effective scope into the delta, so a later WIDENING of the parent does not reach this role — narrowing still does. Requires parent_role
          enum:
            - track
            - pin
          type: string
      required:
        - name
      type: object
    RoleListReply:
      additionalProperties: false
      properties:
        items:
          description: role catalog (metadata + permissions + operators)
          items:
            $ref: "#/components/schemas/RoleView"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    RolePermissionsUpdateRequest:
      additionalProperties: false
      properties:
        confirm_cascade:
          description: proceed even though this change alters the rights of roles DERIVED from this one. Without it such an update is refused 409 with the affected roles and the number of operators holding them; that refusal is what you show the operator before resending with this set
          type: boolean
        default_scope:
          description: "scope: boolean predicate over coven/service/incarnation/host/trait; omitted → scope untouched; present (incl. null) → replaces (null removes scope). On a derived role it is the attenuating delta"
          type:
            - string
            - "null"
        parent_role:
          description: "re-root the role's derivation (ADR-078); omitted → untouched; present (incl. null) → replaces (null makes the role plain again). The ceiling is re-checked on every update: the result must stay within the parent and the caller must hold it"
          type:
            - string
            - "null"
        permissions:
          description: complete new set of permission strings (replace)
          items:
            type: string
          type:
            - array
            - "null"
        scope_mode:
          description: "the delta's intent (ADR-078); omitted → untouched. Sending pin RE-PINS: the parent's effective scope AS OF NOW is written into the delta, so later widenings of the parent stop here. Sending track drops back to following the parent"
          enum:
            - track
            - pin
          type:
            - string
            - "null"
      required:
        - permissions
      type: object
    RoleView:
      additionalProperties: false
      properties:
        builtin:
          type: boolean
        default_scope:
          description: "role scope AS STORED: boolean predicate over coven/service/incarnation/host/trait (e.g. coven in (a, b) AND host matches redis-*); omitted → role without scope. On a derived role this is only the attenuating delta — see effective_scope"
          type: string
        description:
          type: string
        effective_permissions:
          description: "permissions AS RESOLVED against the derivation chain (ADR-078): own ∩ the parent's effective, every scope capped by the chain's ceiling. Equals permissions on a plain role. Consumers read THIS instead of walking parent_role"
          items:
            type: string
          type:
            - array
            - "null"
        effective_scope:
          description: "role scope AS RESOLVED: the parent's effective scope AND this role's delta; omitted → unrestricted. Bare permissions inherit it, as on a plain role"
          type: string
        inert_permissions:
          description: "stored permissions the derivation chain no longer covers (ADR-078): present in permissions, granting NOTHING, because the parent lost the right or narrowed past it. Empty on a plain role. A role with a non-empty permissions list and everything in here grants no access at all"
          items:
            type: string
          type:
            - array
            - "null"
        name:
          pattern: ^[a-z][a-z0-9-]*$
          type: string
        operators:
          items:
            type: string
          type:
            - array
            - "null"
        parent_role:
          description: the role this one derives from (ADR-078) — its ceiling; omitted → a plain role
          pattern: ^[a-z][a-z0-9-]*$
          type: string
        permissions:
          items:
            type: string
          type:
            - array
            - "null"
        scope_mode:
          description: "what default_scope means on this derived role (ADR-078): track = the delta is the added narrowing only, so the parent's scope cascades in; pin = the parent's scope was materialized into the delta, so a later WIDENING of the parent stops here. Omitted → a plain role. Narrowing always cascades, in both modes"
          enum:
            - track
            - pin
          type: string
      required:
        - builtin
        - effective_permissions
        - inert_permissions
        - name
        - operators
        - permissions
      type: object
    RunDetailReply:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        finished_at:
          format: date-time
          type: string
        hosts:
          items:
            $ref: "#/components/schemas/RunHostStatusEntry"
          type:
            - array
            - "null"
        input:
          additionalProperties: {}
          type: object
        scenario:
          type: string
        started_at:
          format: date-time
          type: string
        started_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        status:
          enum:
            - applying
            - success
            - failed
            - cancelled
          type: string
      required:
        - apply_id
        - scenario
        - status
        - started_at
        - hosts
      type: object
    RunHostStatusEntry:
      additionalProperties: false
      properties:
        attempt:
          format: int32
          type: integer
        cancel_requested:
          type: boolean
        error_summary:
          type: string
        failed_plan_index:
          format: int64
          type: integer
        failed_task_idx:
          format: int64
          type: integer
        notices:
          items:
            $ref: "#/components/schemas/RunNoticeEntry"
          type:
            - array
            - "null"
        passage:
          format: int64
          type: integer
        sid:
          description: host FQDN OR synthetic run sid (keeper=on:keeper, __run__=run-sentinel abort to dispatch), not addressing a Soul (NIM-36)
          pattern: ^(keeper|__run__|[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?(\.[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)*)$
          type: string
        status:
          type: string
      required:
        - sid
        - status
        - passage
        - attempt
        - cancel_requested
      type: object
    RunNoticeEntry:
      additionalProperties: false
      properties:
        code:
          type: string
        message:
          type: string
        module:
          type: string
        param:
          type: string
      required:
        - code
        - module
        - message
      type: object
    RunSummaryEntry:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        finished_at:
          format: date-time
          type: string
        scenario:
          type: string
        started_at:
          format: date-time
          type: string
        started_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        status:
          enum:
            - applying
            - success
            - failed
            - cancelled
          type: string
      required:
        - apply_id
        - scenario
        - status
        - started_at
      type: object
    RunTaskEntry:
      additionalProperties: false
      properties:
        hosts:
          items:
            $ref: "#/components/schemas/RunTaskHostEntry"
          type:
            - array
            - "null"
        module:
          type: string
        name:
          type: string
        no_log:
          type: boolean
        params:
          additionalProperties: {}
          type: object
        passage:
          format: int64
          type: integer
        plan_index:
          format: int64
          type: integer
      required:
        - plan_index
        - passage
        - name
        - module
        - no_log
        - hosts
      type: object
    RunTaskErrorEntry:
      additionalProperties: false
      properties:
        code:
          type: string
        message:
          type: string
        module:
          type: string
      required:
        - code
        - module
      type: object
    RunTaskHostEntry:
      additionalProperties: false
      properties:
        error:
          $ref: "#/components/schemas/RunTaskErrorEntry"
        output:
          additionalProperties: {}
          type: object
        sid:
          description: host FQDN OR synthetic run sid (keeper=on:keeper)
          pattern: ^(keeper|__run__|[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?(\.[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)*)$
          type: string
        status:
          enum:
            - TASK_STATUS_UNSPECIFIED
            - TASK_STATUS_OK
            - TASK_STATUS_CHANGED
            - TASK_STATUS_SKIPPED
            - TASK_STATUS_FAILED
            - TASK_STATUS_TIMED_OUT
            - TASK_STATUS_CANCELLED
          type: string
      required:
        - sid
        - status
      type: object
    RunTasksReply:
      additionalProperties: false
      properties:
        tasks:
          items:
            $ref: "#/components/schemas/RunTaskEntry"
          type:
            - array
            - "null"
      required:
        - tasks
      type: object
    RunsListReply:
      additionalProperties: false
      properties:
        items:
          description: page of runs across all incarnations (apply_runs fold)
          items:
            $ref: "#/components/schemas/GlobalRunEntry"
          type:
            - array
            - "null"
        limit:
          description: page size
          format: int32
          type: integer
        offset:
          description: offset from start of set
          format: int32
          type: integer
        total:
          description: total number of runs under filters/scope
          format: int32
          type: integer
      required:
        - items
        - offset
        - limit
        - total
      type: object
    RunsStatsBucket:
      additionalProperties: false
      properties:
        applying:
          description: runs in progress
          format: int64
          type: integer
        cancelled:
          description: cancelled runs
          format: int64
          type: integer
        failed:
          description: failed runs (including orphaned hosts)
          format: int64
          type: integer
        success:
          description: successful runs
          format: int64
          type: integer
        total:
          description: total runs in the bucket
          format: int64
          type: integer
      required:
        - total
        - applying
        - success
        - failed
        - cancelled
      type: object
    RunsStatsReply:
      additionalProperties: false
      properties:
        all:
          $ref: "#/components/schemas/RunsStatsBucket"
          description: all time
        last_24h:
          $ref: "#/components/schemas/RunsStatsBucket"
          description: runs started in the last 24 hours
      required:
        - all
        - last_24h
      type: object
    Scenario:
      additionalProperties: false
      properties:
        composes_name:
          type: boolean
        create:
          type: boolean
        description:
          type: string
        form:
          $ref: "#/components/schemas/ScenarioForm"
        from_versions:
          items:
            type: string
          type:
            - array
            - "null"
        input_schema:
          additionalProperties: {}
          type: object
        kind:
          type: string
        name:
          type: string
        path:
          type: string
        runnable:
          type: boolean
        tags:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - name
        - path
        - kind
        - runnable
      type: object
    ScenarioForm:
      additionalProperties: false
      properties:
        sections:
          items:
            $ref: "#/components/schemas/ScenarioFormSection"
          type:
            - array
            - "null"
      type: object
    ScenarioFormField:
      additionalProperties: false
      properties:
        hint:
          type: string
        label:
          type: string
        name:
          type: string
        placeholder:
          type: string
        show_when:
          type: string
      required:
        - name
      type: object
    ScenarioFormSection:
      additionalProperties: false
      properties:
        collapsed:
          type: boolean
        description:
          type: string
        fields:
          items:
            $ref: "#/components/schemas/ScenarioFormField"
          type:
            - array
            - "null"
        key:
          type: string
        show_when:
          type: string
        title:
          type: string
      required:
        - key
      type: object
    ServiceCompatReply:
      additionalProperties: false
      properties:
        detail:
          type: string
        effective_window:
          $ref: "#/components/schemas/CompatWindowView"
        enforced:
          type: boolean
        entities:
          items:
            $ref: "#/components/schemas/CompatEntityView"
          type:
            - array
            - "null"
        keeper_release:
          type: string
        keeper_version:
          type: string
        ref:
          type: string
        service:
          type: string
        sha1:
          type: string
        status:
          type: string
      required:
        - service
        - ref
        - sha1
        - keeper_version
        - enforced
        - status
        - effective_window
        - entities
      type: object
    ServiceDependenciesReply:
      additionalProperties: false
      properties:
        destiny:
          items:
            $ref: "#/components/schemas/ServiceDependency"
          type:
            - array
            - "null"
        modules:
          items:
            $ref: "#/components/schemas/ServiceDependency"
          type:
            - array
            - "null"
        ref:
          type: string
        service:
          type: string
      required:
        - destiny
        - modules
        - ref
        - service
      type: object
    ServiceDependency:
      additionalProperties: false
      properties:
        git:
          type: string
        name:
          type: string
        ref:
          type: string
      required:
        - name
        - ref
      type: object
    ServiceDirectivesReply:
      additionalProperties: false
      properties:
        directives:
          additionalProperties:
            items:
              type: string
            type:
              - array
              - "null"
          type: object
        ref:
          type: string
        service:
          type: string
        sha1:
          type: string
      required:
        - service
        - ref
        - sha1
        - directives
      type: object
    ServiceListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ServiceView"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    ServiceRefsListReply:
      additionalProperties: false
      properties:
        refs:
          items:
            $ref: "#/components/schemas/GitRef"
          type:
            - array
            - "null"
        service:
          type: string
      required:
        - refs
        - service
      type: object
    ServiceRegisterRequest:
      additionalProperties: false
      properties:
        git:
          description: git source of the service repo (URL; not a secret)
          type: string
        name:
          description: Service name (kebab-case)
          pattern: ^[a-z][a-z0-9-]*$
          type: string
        ref:
          description: git ref (tag/branch) - Service version (ADR-007)
          type: string
        refresh:
          description: opt. auto-refresh duration ('5m'); omitted - no auto-refresh
          type: string
      required:
        - name
        - git
        - ref
      type: object
    ServiceScenariosListReply:
      additionalProperties: false
      properties:
        ref:
          description: git-ref the listing was produced from
          type: string
        scenarios:
          description: scenario from the Service git-repo snapshot
          items:
            $ref: "#/components/schemas/Scenario"
          type:
            - array
            - "null"
        service:
          description: Service name (duplicate of path parameter)
          type: string
      required:
        - service
        - ref
        - scenarios
      type: object
    ServiceStateSchemaReply:
      additionalProperties: false
      properties:
        migrations:
          items:
            $ref: "#/components/schemas/StateSchemaMigration"
          type:
            - array
            - "null"
        ref:
          type: string
        schema:
          additionalProperties: {}
          type: object
        service:
          type: string
        state_schema_version:
          format: int64
          type: integer
      required:
        - migrations
        - ref
        - service
        - state_schema_version
      type: object
    ServiceTelemetryReply:
      additionalProperties: false
      properties:
        collectors:
          items:
            type: string
          type:
            - array
            - "null"
        enabled:
          type: boolean
        interval_sec:
          format: int32
          type: integer
        known_collectors:
          items:
            type: string
          type:
            - array
            - "null"
        ref:
          type: string
        service:
          type: string
        sha1:
          type: string
      required:
        - service
        - ref
        - sha1
        - enabled
        - interval_sec
        - collectors
        - known_collectors
      type: object
    ServiceUpdateRequest:
      additionalProperties: false
      properties:
        git:
          description: new git source
          type: string
        ref:
          description: new git ref
          type: string
        refresh:
          description: opt. auto-refresh duration ('5m')
          type: string
      required:
        - git
        - ref
      type: object
    ServiceView:
      additionalProperties: false
      properties:
        created_at:
          format: date-time
          type: string
        created_by_aid:
          type: string
        git:
          type: string
        name:
          pattern: ^[a-z][a-z0-9-]*$
          type: string
        ref:
          type: string
        refresh:
          type: string
        updated_at:
          format: date-time
          type: string
        updated_by_aid:
          type: string
      required:
        - created_at
        - git
        - name
        - ref
        - updated_at
      type: object
    SettingReply:
      additionalProperties: false
      properties:
        bounds:
          description: accepted range, e.g. (0, 1] or [30s, 1h]; an enum ("debug | info | warn | error") or "true | false" where a range makes no sense
          type: string
        cluster_value:
          description: value stored cluster-wide, present only when this instance's file shadows it
        default: {}
        description:
          type: string
        key:
          type: string
        overridden_locally:
          description: true when keeper.yml on this instance wins over the cluster value
          type: boolean
        source:
          enum:
            - default
            - file
            - pg
          type: string
        type:
          enum:
            - float
            - int
            - duration
            - bool
            - string
          type: string
        value:
          description: effective value on this instance
        yaml_path:
          type: string
      required:
        - key
        - yaml_path
        - type
        - bounds
        - default
        - value
        - source
        - description
      type: object
    SettingUpdateRequest:
      additionalProperties: false
      properties:
        value:
          description: new value in text form (e.g. "0.5"); parsed and range-checked by the field registry
          type: string
      required:
        - value
      type: object
    SettingsCatalogReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/SettingReply"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    SigilKeyIntroduceReply:
      additionalProperties: false
      properties:
        introduced_at:
          format: date-time
          type: string
        is_primary:
          type: boolean
        key_id:
          pattern: ^[0-9a-f]{64}$
          type: string
        pubkey_pem:
          type: string
        status:
          type: string
      required:
        - introduced_at
        - is_primary
        - key_id
        - pubkey_pem
        - status
      type: object
    SigilKeyIntroduceRequest:
      additionalProperties: false
      properties:
        make_primary:
          description: make the new key primary (new Sigils are signed with it); default false
          type: boolean
      type: object
    SigilKeyListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/SigilKeyView"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    SigilKeyView:
      additionalProperties: false
      properties:
        introduced_at:
          format: date-time
          type: string
        is_primary:
          type: boolean
        key_id:
          pattern: ^[0-9a-f]{64}$
          type: string
        status:
          type: string
      required:
        - introduced_at
        - is_primary
        - key_id
        - status
      type: object
    SoulCovenAssignReply:
      additionalProperties: false
      properties:
        changed:
          description: number of rows actually changed
          format: int32
          type: integer
        dry_run:
          description: dry-run without writing
          type: boolean
        label:
          description: applied label for append/remove (mirror input)
          type: string
        labels:
          description: applied set tokens for replace (mirror input)
          items:
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        matched:
          description: number of hosts matching selector ∩ scope
          format: int32
          type: integer
        mode:
          description: operation type on covens[]
          type: string
        status:
          description: completed — all chunks committed; partial — mid-run failure
          enum:
            - completed
            - partial
          type: string
      required:
        - mode
        - label
        - matched
        - changed
        - status
        - dry_run
      type: object
    SoulCovenAssignRequest:
      additionalProperties: false
      properties:
        dry_run:
          description: count matched without UPDATE
          type: boolean
        label:
          description: label for append/remove (forbidden for replace)
          maxLength: 63
          type: string
        labels:
          description: set for replace (may be empty = remove all; forbidden for append/remove)
          items:
            maxLength: 63
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        mode:
          description: append — add label; remove — remove; replace — replace set
          enum:
            - append
            - remove
            - replace
          type: string
        selector:
          $ref: "#/components/schemas/SoulCovenAssignSelector"
          description: targeting (at least one criterion; AND combinations)
      required:
        - mode
        - selector
      type: object
    SoulCovenAssignSelector:
      additionalProperties: false
      properties:
        all:
          description: no host filter (entire registry ∩ scope)
          type: boolean
        coven:
          description: hosts carrying this Coven tag on themselves; belonging to an incarnation attaches no tag (NIM-281)
          maxLength: 63
          type: string
        incarnation:
          description: members of this incarnation, resolved from incarnation_membership — a membership question, never answered from the tags above (ADR-008 amendment NIM-124)
          maxLength: 63
          type: string
        sids:
          description: point list of hosts (SID = FQDN)
          items:
            type: string
          type:
            - array
            - "null"
        status:
          description: Soul status in registry
          enum:
            - pending
            - connected
            - disconnected
            - revoked
            - expired
            - destroyed
          type: string
      type: object
    SoulCreateReply:
      additionalProperties: false
      properties:
        bootstrap_token:
          type: string
        covens:
          items:
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type: array
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        expires_at:
          format: date-time
          type: string
        registered_at:
          format: date-time
          type: string
        sid:
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
        status:
          $ref: "#/components/schemas/SoulStatus"
        transport:
          $ref: "#/components/schemas/SoulTransport"
      required:
        - created_by_aid
        - registered_at
        - sid
        - status
        - transport
      type: object
    SoulCreateRequest:
      additionalProperties: false
      properties:
        covens:
          description: stable Coven tags of host (kebab-case, ADR-008)
          items:
            maxLength: 63
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        note:
          description: server-only note (souls.note)
          type: string
        sid:
          description: SID of new host = FQDN
          type: string
        transport:
          description: "delivery method: agent (mTLS gRPC stream) / ssh (push without agent)"
          enum:
            - agent
            - ssh
          type: string
      required:
        - sid
        - transport
      type: object
    SoulHistoryItem:
      additionalProperties: false
      properties:
        finished_at:
          format: date-time
          type: string
        id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        incarnation:
          type: string
        module:
          type: string
        scenario:
          type: string
        started_at:
          format: date-time
          type: string
        status:
          type: string
        type:
          type: string
        voyage_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - id
        - started_at
        - status
        - type
      type: object
    SoulHistoryReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/SoulHistoryItem"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        sid:
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - sid
        - total
      type: object
    SoulIssueTokenReply:
      additionalProperties: false
      properties:
        bootstrap_token:
          type: string
        expires_at:
          format: date-time
          type: string
        sid:
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
      required:
        - bootstrap_token
        - expires_at
        - sid
      type: object
    SoulListEntry:
      additionalProperties: false
      properties:
        covens:
          items:
            pattern: ^[a-z][a-z0-9]*(-[a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        created_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type:
            - string
            - "null"
        last_seen_at:
          format: date-time
          type:
            - string
            - "null"
        last_seen_by_kid:
          type:
            - string
            - "null"
        registered_at:
          format: date-time
          type: string
        requested_at:
          format: date-time
          type:
            - string
            - "null"
        sid:
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
        status:
          $ref: "#/components/schemas/SoulStatus"
        traits:
          additionalProperties: {}
          description: operator-set key→value labels (ADR-060); value - scalar or list of scalars; bare-soul → {}
          type: object
        transport:
          $ref: "#/components/schemas/SoulTransport"
      required:
        - covens
        - traits
        - created_by_aid
        - last_seen_at
        - last_seen_by_kid
        - registered_at
        - requested_at
        - sid
        - status
        - transport
      type: object
    SoulListReply:
      additionalProperties: false
      properties:
        items:
          description: page of the souls registry
          items:
            $ref: "#/components/schemas/SoulListEntry"
          type:
            - array
            - "null"
        limit:
          description: page size
          format: int32
          type: integer
        next_cursor:
          description: opaque keyset cursor for the next page (keyset mode); absent in offset mode and when the set is exhausted
          type: string
        offset:
          description: offset from start of set (offset mode)
          format: int32
          type: integer
        total:
          description: total record count; meaningful only in offset mode
          format: int32
          type: integer
        total_approximate:
          description: total is NOT exact (keyset mode); omitted in offset mode
          type: boolean
      required:
        - items
        - offset
        - limit
        - total
      type: object
    SoulSshTarget:
      additionalProperties: false
      properties:
        soul_path:
          description: absolute install path of the soul binary (starts with /)
          pattern: ^/
          type: string
        ssh_port:
          description: SSH port [1..65535]
          format: int64
          maximum: 65535
          minimum: 1
          type: integer
        ssh_provider:
          description: opt. SshProvider name (3-tier routing); empty -> coven/cluster default
          type: string
        ssh_user:
          description: SSH user
          minLength: 1
          type: string
      required:
        - soul_path
        - ssh_port
        - ssh_user
      type: object
    SoulSshTargetReply:
      additionalProperties: false
      properties:
        sid:
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
        ssh_target:
          $ref: "#/components/schemas/SoulSshTarget"
      required:
        - sid
        - ssh_target
      type: object
    SoulStatsReply:
      additionalProperties: false
      properties:
        by_coven:
          additionalProperties:
            format: int64
            type: integer
          type: object
        by_status:
          additionalProperties:
            format: int64
            type: integer
          type: object
        by_transport:
          additionalProperties:
            format: int64
            type: integer
          type: object
        stale_count:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - by_status
        - by_transport
        - by_coven
        - total
        - stale_count
      type: object
    SoulStatus:
      description: Soul status in the registry.
      enum:
        - pending
        - connected
        - disconnected
        - revoked
        - expired
        - destroyed
      type: string
    SoulTelemetryReply:
      additionalProperties: false
      properties:
        collected_at:
          description: Soul-side collection moment
          format: date-time
          type: string
        latest:
          $ref: "#/components/schemas/UtilizationLatest"
          description: the latest snapshot (nil if there is no data)
        received_at:
          description: Keeper-side receive moment
          format: date-time
          type: string
        sid:
          description: SID (FQDN) of the Soul
          type: string
        stale:
          description: true if the snapshot is stale (age > TTL) or there is no data
          type: boolean
        window:
          description: a window of points for sparklines, newest-first
          items:
            $ref: "#/components/schemas/UtilizationWindowPoint"
          type:
            - array
            - "null"
      required:
        - sid
        - stale
      type: object
    SoulTraitsAssignReply:
      additionalProperties: false
      properties:
        changed:
          description: number of rows actually changed
          format: int32
          type: integer
        dry_run:
          description: dry-run without writing
          type: boolean
        keys:
          description: affected trait keys (mirror input)
          items:
            pattern: ^[a-z][a-z0-9]*([_-][a-z0-9]+)*$
            type: string
          type:
            - array
            - "null"
        matched:
          description: number of hosts matching selector ∩ scope
          format: int32
          type: integer
        mode:
          description: operation mode (merge/replace/remove)
          type: string
        status:
          description: completed — all chunks committed; partial — mid-run failure
          enum:
            - completed
            - partial
          type: string
      required:
        - mode
        - keys
        - matched
        - changed
        - status
        - dry_run
      type: object
    SoulTraitsAssignRequest:
      additionalProperties: false
      properties:
        dry_run:
          description: count matched without UPDATE
          type: boolean
        keys:
          description: list of key names for remove (kebab-case); forbidden for merge/replace
          items:
            type: string
          type:
            - array
            - "null"
        mode:
          description: merge (default) - set/overwrite keys; replace - replace the whole map; remove - delete keys from keys
          enum:
            - merge
            - replace
            - remove
          type: string
        selector:
          $ref: "#/components/schemas/SoulCovenAssignSelector"
          description: targeting (at least one criterion; AND combinations)
        traits:
          additionalProperties: {}
          description: key->value set for merge/replace (value - scalar or list of scalars); forbidden for remove
          type: object
      required:
        - selector
      type: object
    SoulTransport:
      description: Configuration delivery method. agent — soul daemon on top of mTLS gRPC stream; ssh — agentless push.
      enum:
        - agent
        - ssh
      type: string
    SoulprintCpuFacts:
      additionalProperties: false
      properties:
        count:
          description: number of logical CPUs (accounting for HT/SMT)
          format: int32
          type: integer
        model:
          type: string
        vendor:
          type: string
      type: object
    SoulprintFacts:
      additionalProperties: false
      properties:
        cpu:
          $ref: "#/components/schemas/SoulprintCpuFacts"
        hostname:
          description: short hostname, uname -n
          type: string
        kernel:
          $ref: "#/components/schemas/SoulprintKernelFacts"
        memory:
          $ref: "#/components/schemas/SoulprintMemoryFacts"
          description: memory amounts in MB
        network:
          $ref: "#/components/schemas/SoulprintNetworkFacts"
        os:
          $ref: "#/components/schemas/SoulprintOsFacts"
          description: operating-system facts (ADR-018)
        sid:
          description: echo SID for logs; authority - mTLS peer cert
          type: string
      type: object
    SoulprintKernelFacts:
      additionalProperties: false
      properties:
        release:
          description: kernel version only (5.15.0)
          type: string
        version:
          description: full version with dist-suffix (5.15.0-101-generic)
          type: string
      type: object
    SoulprintMemoryFacts:
      additionalProperties: false
      properties:
        available_mb:
          format: int64
          type: integer
        swap_mb:
          format: int64
          type: integer
        total_mb:
          format: int64
          type: integer
      type: object
    SoulprintNetworkFacts:
      additionalProperties: false
      properties:
        fqdn:
          type: string
        interfaces:
          items:
            $ref: "#/components/schemas/SoulprintNetworkInterface"
          type: array
        primary_ip:
          description: primary IPv4 (interface with default route)
          type: string
      type: object
    SoulprintNetworkInterface:
      additionalProperties: false
      properties:
        ipv4:
          description: IPv4 addresses in CIDR (10.0.0.1/24)
          items:
            type: string
          type: array
        ipv6:
          items:
            type: string
          type: array
        mac:
          type: string
        mtu:
          format: int32
          type: integer
        name:
          type: string
      type: object
    SoulprintOsFacts:
      additionalProperties: false
      properties:
        arch:
          description: amd64 / arm64
          type: string
        codename:
          type: string
        distro:
          type: string
        family:
          description: debian / rhel / alpine / windows / darwin
          type: string
        init_system:
          description: systemd / openrc / sysv / launchd
          type: string
        pkg_mgr:
          description: apt / dnf / apk / pacman
          type: string
        version:
          type: string
      type: object
    SoulprintReadReply:
      additionalProperties: false
      properties:
        collected_at:
          description: Soul-side timestamp of fact collection
          format: date-time
          type: string
        received_at:
          description: Keeper-side timestamp of stream receipt
          format: date-time
          type: string
        sid:
          description: SID (FQDN) of Soul
          type: string
        typed_facts:
          $ref: "#/components/schemas/SoulprintFacts"
          description: typed Soulprint facts (ADR-018); byte-passthrough JSONB on wire, shaped per proto SoulprintFacts
      required:
        - sid
        - typed_facts
      type: object
    StateHistoryEntry:
      additionalProperties: false
      properties:
        apply_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        changed_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        created_at:
          format: date-time
          type: string
        history_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
        scenario:
          type: string
        state_after:
          additionalProperties: {}
          type: object
        state_before:
          additionalProperties: {}
          type: object
      required:
        - apply_id
        - created_at
        - history_id
        - scenario
        - state_after
        - state_before
      type: object
    StateSchemaMigration:
      additionalProperties: false
      properties:
        from:
          format: int64
          type: integer
        path:
          type: string
        to:
          format: int64
          type: integer
      required:
        - from
        - path
        - to
      type: object
    Subject:
      additionalProperties: false
      properties:
        coven:
          description: Coven labels — reaches a host carrying one of them, and every member of an incarnation carrying one
          items:
            type: string
          type:
            - array
            - "null"
        incarnation:
          $ref: "#/components/schemas/SubjectIncarnation"
          description: service+name pair — reaches every host that is a member of that incarnation
        sid:
          description: exact SIDs — reaches those hosts and nothing else
          items:
            type: string
          type:
            - array
            - "null"
        trait:
          $ref: "#/components/schemas/SubjectTrait"
          description: trait key/value — the same two-level reach as coven, on the traits map
      type: object
    SubjectIncarnation:
      additionalProperties: false
      properties:
        name:
          description: incarnation name (unique within the service)
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        service:
          description: service the incarnation belongs to
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
      required:
        - name
        - service
      type: object
    SubjectTrait:
      additionalProperties: false
      properties:
        key:
          description: trait key
          pattern: ^[a-z][a-z0-9_.-]*$
          type: string
        value:
          description: trait value; a scalar trait matches by text, a list trait by membership
          type: string
      required:
        - key
        - value
      type: object
    SynodCreateRequest:
      additionalProperties: false
      properties:
        description:
          description: human-readable group description for UI/audit
          maxLength: 1024
          type: string
        name:
          description: Synod group name (kebab-case), unique cluster-wide
          pattern: ^[a-z][a-z0-9-]*$
          type: string
      required:
        - name
      type: object
    SynodGrantRoleRequest:
      additionalProperties: false
      properties:
        role:
          description: name of the role being added to the group bundle
          type: string
      required:
        - role
      type: object
    SynodListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/SynodView"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    SynodUpdateRequest:
      additionalProperties: false
      properties:
        description:
          description: new human-readable group description for UI/audit
          maxLength: 1024
          minLength: 1
          type: string
      required:
        - description
      type: object
    SynodView:
      additionalProperties: false
      properties:
        builtin:
          type: boolean
        description:
          type: string
        name:
          pattern: ^[a-z][a-z0-9-]*$
          type: string
        operators:
          items:
            pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
            type: string
          type:
            - array
            - "null"
        roles:
          items:
            pattern: ^[a-z][a-z0-9-]*$
            type: string
          type:
            - array
            - "null"
      required:
        - builtin
        - name
        - operators
        - roles
      type: object
    TelemetryDisk:
      additionalProperties: false
      properties:
        inodes_total:
          format: int64
          type: integer
        inodes_used:
          format: int64
          type: integer
        mount:
          type: string
        total_mb:
          format: int64
          type: integer
        used_mb:
          format: int64
          type: integer
      required:
        - mount
        - used_mb
        - total_mb
        - inodes_used
        - inodes_total
      type: object
    Tiding:
      additionalProperties: false
      properties:
        annotations:
          additionalProperties: {}
          type: object
        cadence:
          type: string
        created_at:
          format: date-time
          type: string
        created_by_aid:
          type: string
        enabled:
          type: boolean
        ephemeral:
          type: boolean
        event_types:
          items:
            type: string
          type:
            - array
            - "null"
        herald:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        incarnation:
          type: string
        name:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        only_changes:
          type: boolean
        only_failures:
          type: boolean
        projection:
          items:
            type: string
          type: array
        task:
          type: string
        updated_at:
          format: date-time
          type: string
        voyage_id:
          type: string
      required:
        - created_at
        - enabled
        - event_types
        - herald
        - name
        - only_changes
        - only_failures
        - updated_at
      type: object
    TidingCreateRequest:
      additionalProperties: false
      properties:
        annotations:
          additionalProperties: {}
          description: static operator fields, merged into the webhook body under the annotations key
          type: object
        cadence:
          description: opt. selector binding to the source Cadence schedule
          type: string
        enabled:
          description: rule enabled (omitted → true)
          type: boolean
        event_types:
          description: list of event-types in run scope (area-glob or exact); empty -> 422
          items:
            type: string
          type:
            - array
            - "null"
        herald:
          description: Herald channel name for delivery (FK on heralds.name)
          type: string
        incarnation:
          description: opt. selector binding to the source incarnation
          type: string
        name:
          description: Tiding rule name (kebab-case, 1..63)
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        only_changes:
          description: deliver only on changes (omitted → false)
          type: boolean
        only_failures:
          description: deliver only failures (omitted → false)
          type: boolean
        projection:
          description: allow-list of payload paths; empty/omitted — full form
          items:
            type: string
          type: array
        task:
          description: opt. selector subscribing to a specific task (register ∪ id from changed_tasks)
          type: string
      required:
        - name
        - herald
        - event_types
      type: object
    TidingListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/Tiding"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    TidingUpdateRequest:
      additionalProperties: false
      properties:
        annotations:
          additionalProperties: {}
          description: static operator fields (replace — absence clears)
          type: object
        cadence:
          description: opt. selector binding to the Cadence; absence clears it
          type: string
        enabled:
          description: rule enabled (omitted → true)
          type: boolean
        event_types:
          description: list of event-types in run scope (replace)
          items:
            type: string
          type:
            - array
            - "null"
        herald:
          description: Herald channel name for delivery (FK)
          type: string
        incarnation:
          description: opt. selector binding to the incarnation; absence clears it
          type: string
        only_changes:
          description: deliver only on changes (omitted → false)
          type: boolean
        only_failures:
          description: deliver only failures (omitted → false)
          type: boolean
        projection:
          description: allow-list of payload paths (replace — absence = full form)
          items:
            type: string
          type: array
        task:
          description: opt. selector subscribing to a task; absence clears it
          type: string
      required:
        - herald
        - event_types
      type: object
    UpgradePathRef:
      additionalProperties: false
      properties:
        commit:
          type: string
        is_current:
          type: boolean
        ref:
          type: string
        type:
          type: string
      required:
        - ref
        - type
        - commit
        - is_current
      type: object
    UpgradePathTarget:
      additionalProperties: false
      properties:
        direction:
          type: string
        downgrade:
          type: boolean
        mode:
          type: string
        reachable:
          type: boolean
        resolved_commit:
          type: string
        slug:
          type: string
        state_migrations:
          items:
            $ref: "#/components/schemas/StateSchemaMigration"
          type:
            - array
            - "null"
        target_state_schema_version:
          format: int64
          type: integer
        to:
          type: string
        unreachable_reason:
          type: string
      required:
        - to
        - resolved_commit
        - target_state_schema_version
        - direction
        - downgrade
        - reachable
      type: object
    UtilizationLatest:
      additionalProperties: false
      properties:
        cpu_pct:
          format: double
          type: number
        disks:
          items:
            $ref: "#/components/schemas/TelemetryDisk"
          type:
            - array
            - "null"
        interval_sec:
          format: int32
          type: integer
        load1:
          format: double
          type: number
        load15:
          format: double
          type: number
        load5:
          format: double
          type: number
        mem_total_mb:
          format: int64
          type: integer
        mem_used_mb:
          format: int64
          type: integer
        net_err_ps:
          format: int64
          type: integer
        net_rx_bps:
          format: int64
          type: integer
        net_tx_bps:
          format: int64
          type: integer
        swap_used_mb:
          format: int64
          type: integer
        uptime_sec:
          format: int64
          type: integer
      required:
        - cpu_pct
        - load1
        - load5
        - load15
        - mem_used_mb
        - mem_total_mb
        - swap_used_mb
        - uptime_sec
        - net_rx_bps
        - net_tx_bps
        - net_err_ps
        - interval_sec
      type: object
    UtilizationWindowPoint:
      additionalProperties: false
      properties:
        collected_at:
          format: date-time
          type: string
        cpu_pct:
          format: double
          type: number
        load1:
          format: double
          type: number
        mem_total_mb:
          format: int64
          type: integer
        mem_used_mb:
          format: int64
          type: integer
        net_rx_bps:
          format: int64
          type: integer
        net_tx_bps:
          format: int64
          type: integer
      required:
        - collected_at
        - cpu_pct
        - load1
        - mem_used_mb
        - mem_total_mb
        - net_rx_bps
        - net_tx_bps
      type: object
    VigilCreateRequest:
      additionalProperties: false
      properties:
        check:
          description: core-beacon address (e.g. 'core.beacon.file_changed')
          type: string
        enabled:
          description: whether the check is active (default true)
          type: boolean
        interval:
          description: check frequency (duration convention, e.g. '30s')
          type: string
        name:
          description: Vigil name (kebab-case, 1..63)
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        params:
          description: check parameters; shape depends on check (passed through as-is)
        subject:
          $ref: "#/components/schemas/Subject"
          description: which hosts run the check — exactly one of sid / incarnation / coven / trait
      required:
        - name
        - subject
        - interval
        - check
      type: object
    VigilListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/VigilView"
          type:
            - array
            - "null"
        limit:
          format: int32
          type: integer
        offset:
          format: int32
          type: integer
        total:
          format: int32
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    VigilView:
      additionalProperties: false
      properties:
        check:
          type: string
        created_at:
          format: date-time
          type: string
        created_by_aid:
          type: string
        enabled:
          type: boolean
        interval:
          type: string
        name:
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        params: {}
        subject:
          $ref: "#/components/schemas/Subject"
        updated_at:
          format: date-time
          type: string
      required:
        - check
        - created_at
        - enabled
        - interval
        - name
        - params
        - subject
        - updated_at
      type: object
    Voice:
      additionalProperties: false
      properties:
        added_at:
          format: date-time
          type: string
        added_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type:
            - string
            - "null"
        choir_name:
          type: string
        incarnation_name:
          pattern: ^[a-z0-9][a-z0-9-]{0,62}$
          type: string
        position:
          format: int64
          type:
            - integer
            - "null"
        role:
          type:
            - string
            - "null"
        sid:
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
      required:
        - added_at
        - added_by_aid
        - choir_name
        - incarnation_name
        - position
        - role
        - sid
      type: object
    VoiceAddRequest:
      additionalProperties: false
      properties:
        position:
          description: ordinal index in the batch (>= 0)
          format: int64
          type: integer
        role:
          description: declared-role (kebab-case, 1..63)
          maxLength: 63
          type: string
        sid:
          description: SID (FQDN) of a host - a member of the incarnation
          pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
          type: string
      required:
        - sid
      type: object
    VoiceListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/Voice"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    Voyage:
      additionalProperties: false
      properties:
        attempt:
          format: int64
          type: integer
        batch_mode:
          type: string
        batch_percent:
          format: int64
          type: integer
        batch_size:
          format: int64
          type: integer
        concurrency:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        current_batch_index:
          format: int64
          type: integer
        dry_run:
          type: boolean
        fail_threshold:
          format: int64
          type: integer
        finished_at:
          format: date-time
          type: string
        kind:
          type: string
        module:
          type: string
        on_failure:
          type: string
        require_alive:
          type: boolean
        scenario_name:
          type: string
        schedule_at:
          format: date-time
          type: string
        scope_size:
          format: int64
          type: integer
        started_at:
          format: date-time
          type: string
        started_by_aid:
          pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
          type: string
        status:
          type: string
        summary:
          $ref: "#/components/schemas/VoyageSummary"
        target:
          $ref: "#/components/schemas/VoyageTarget"
        total_batches:
          format: int64
          type: integer
        voyage_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - attempt
        - created_at
        - current_batch_index
        - dry_run
        - kind
        - scope_size
        - started_by_aid
        - status
        - total_batches
        - voyage_id
      type: object
    VoyageCancelReply:
      additionalProperties: false
      properties:
        status:
          type: string
        voyage_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - status
        - voyage_id
      type: object
    VoyageCreateReply:
      additionalProperties: false
      properties:
        kind:
          type: string
        location:
          type: string
        scope_size:
          format: int64
          type: integer
        status:
          type: string
        voyage_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - kind
        - location
        - scope_size
        - status
        - voyage_id
      type: object
    VoyageCreateRequest:
      additionalProperties: false
      properties:
        batch:
          description: "batch size: N hosts or N%"
          type: string
        batch_mode:
          description: barrier (default) | window
          type: string
        batch_percent:
          format: int64
          maximum: 100
          minimum: 1
          type: integer
        batch_size:
          format: int64
          minimum: 1
          type: integer
        concurrency:
          format: int64
          maximum: 500
          minimum: 1
          type: integer
        dry_run:
          type: boolean
        fail_threshold:
          format: int64
          minimum: 1
          type: integer
        input:
          additionalProperties: {}
          description: recipe parameters
          type: object
        inter_batch_interval_ms:
          format: int64
          type: integer
        inter_unit_interval_ms:
          format: int64
          type: integer
        kind:
          description: recipe type of the run
          enum:
            - scenario
            - command
          type: string
        max_failures:
          description: "failure threshold: N absolute or N%"
          type: string
        module:
          description: module for kind=command
          type: string
        notify:
          description: one-time subscriptions for THIS run (ephemeral)
          items:
            $ref: "#/components/schemas/VoyageNotify"
          type:
            - array
            - "null"
        on_failure:
          description: abort | continue (default)
          type: string
        require_alive:
          type: boolean
        scenario_name:
          description: scenario name for kind=scenario
          type: string
        schedule_at:
          description: RFC3339 deferred start
          format: date-time
          type: string
        target:
          $ref: "#/components/schemas/VoyageTarget"
          description: declarative target (resolved into a snapshot of units)
      required:
        - kind
        - target
      type: object
    VoyageListReply:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/Voyage"
          type:
            - array
            - "null"
        limit:
          format: int64
          type: integer
        offset:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - limit
        - offset
        - total
      type: object
    VoyageNotify:
      additionalProperties: false
      properties:
        annotations:
          additionalProperties: {}
          type: object
        herald:
          description: herald channel name
          pattern: ^[a-z0-9-]{1,63}$
          type: string
        "on":
          description: "terminal event types: completed|failed|partial"
          items:
            type: string
          type:
            - array
            - "null"
        only_changes:
          type: boolean
        only_failures:
          type: boolean
        projection:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - herald
      type: object
    VoyagePreviewReply:
      additionalProperties: false
      properties:
        batch_mode:
          type: string
        effective_batch_size:
          format: int64
          type: integer
        kind:
          type: string
        scope_size:
          format: int64
          type: integer
        total_batches:
          format: int64
          type: integer
      required:
        - batch_mode
        - kind
        - scope_size
        - total_batches
      type: object
    VoyageSummary:
      additionalProperties: false
      properties:
        cancelled:
          format: int64
          type: integer
        failed:
          format: int64
          type: integer
        no_match:
          format: int64
          type: integer
        succeeded:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - cancelled
        - failed
        - succeeded
        - total
      type: object
    VoyageTarget:
      additionalProperties: false
      properties:
        coven:
          description: coven labels (scenario env-tag / command host label)
          items:
            type: string
          type:
            - array
            - "null"
        incarnations:
          description: incarnation names (scenario mode)
          items:
            type: string
          type:
            - array
            - "null"
        service:
          description: service name (scenario mode)
          type: string
        sids:
          description: host SIDs (command mode)
          items:
            type: string
          type:
            - array
            - "null"
        where:
          description: CEL predicate as an ADDITION to sids/coven (command mode)
          type: string
      type: object
    VoyageTargetEntry:
      additionalProperties: false
      properties:
        apply_id:
          type: string
        batch_index:
          format: int64
          type: integer
        errand_id:
          type: string
        finished_at:
          format: date-time
          type: string
        status:
          type: string
        target_id:
          type: string
        target_kind:
          type: string
      required:
        - batch_index
        - status
        - target_id
        - target_kind
      type: object
    VoyageTargetsReply:
      additionalProperties: false
      properties:
        targets:
          items:
            $ref: "#/components/schemas/VoyageTargetEntry"
          type:
            - array
            - "null"
        voyage_id:
          pattern: ^[0-9A-HJKMNP-TV-Z]{26}$
          type: string
      required:
        - targets
        - voyage_id
      type: object
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      description: "Archon JWT (Authorization: Bearer <jwt>). All /v1 operations require a valid token."
      scheme: bearer
      type: http
info:
  title: Soul Stack Keeper Operator API
  version: v1
openapi: 3.1.0
paths:
  /auth/ldap/login:
    post:
      description: "Federated authentication (ADR-058): LDAP search-bind -> mapping onto operators(aid)+roles -> internal JWT in HttpOnly+Secure cookie. Response body is empty."
      operationId: ldapLogin
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LDAPLoginRequest"
        required: true
      responses:
        "204":
          description: No Content
          headers:
            Set-Cookie:
              schema:
                type: string
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Operator login via LDAP
      tags:
        - auth
  /auth/methods:
    get:
      description: Public list of login methods for the UI login form (password always true; ldap/oidc — depending on keeper config). Booleans only, no IdP URLs/domains.
      operationId: authMethods
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthMethodsReply"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Error
      summary: Available login methods
      tags:
        - auth
  /auth/oidc/callback:
    get:
      description: Validates id_token (JWKS signature/iss/aud/exp/nonce), maps to operators(aid)+roles, issues the internal JWT in an HttpOnly+Secure cookie and redirects (302) to the UI. Validation/mapping error -> 401/403.
      operationId: oidcCallback
      parameters:
        - description: authorization code from the IdP
          explode: false
          in: query
          name: code
          schema:
            description: authorization code from the IdP
            type: string
        - description: opaque CSRF-state, issued at /auth/oidc/login
          explode: false
          in: query
          name: state
          schema:
            description: opaque CSRF-state, issued at /auth/oidc/login
            type: string
        - description: error code from the IdP (if authentication was rejected)
          explode: false
          in: query
          name: error
          schema:
            description: error code from the IdP (if authentication was rejected)
            type: string
      responses:
        "302":
          description: Found
          headers:
            Location:
              schema:
                type: string
            Set-Cookie:
              schema:
                type: string
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: OIDC callback for the operator
      tags:
        - auth
  /auth/oidc/login:
    get:
      description: "Federated authentication (ADR-058): generates state+nonce+PKCE and redirects (302) to the external IdP's authorization_endpoint."
      operationId: oidcLogin
      responses:
        "302":
          description: Found
          headers:
            Location:
              schema:
                type: string
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Start OIDC login for the operator
      tags:
        - auth
  /auth/token:
    post:
      description: "NIM-77/ADR-058 (Variant B): reads the HttpOnly cookie soul_session (internal JWT), verifies it with the same verifier as the Bearer on /v1, and issues a short-lived Bearer in the JSON body. Subject/roles are strictly from verified cookie claims. Not written to audit."
      operationId: authTokenExchange
      parameters:
        - in: cookie
          name: soul_session
          schema:
            type: string
        - in: header
          name: Sec-Fetch-Site
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthTokenReply"
          description: OK
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Exchange session-cookie for a short Bearer
      tags:
        - auth
  /v1/audit:
    get:
      description: Read-only feed of audit_log with filters (type/source multi-OR, archon_aid/correlation_id case-insensitive substring ILIKE, payload_herald/payload_voyage exact, started_after/before RFC3339) and pagination. Permission audit.read. Read-only, no audit (reads are not written - recursion).
      operationId: listAuditEvents
      parameters:
        - description: multi-value ?type=X&type=Y - exact-match OR by event_type
          explode: true
          in: query
          name: type
          schema:
            description: multi-value ?type=X&type=Y - exact-match OR by event_type
            items:
              type: string
            type:
              - array
              - "null"
        - description: multi-value ?source=api&source=mcp - exact-match OR; value outside enum -> 422
          explode: true
          in: query
          name: source
          schema:
            description: multi-value ?source=api&source=mcp - exact-match OR; value outside enum -> 422
            items:
              enum:
                - signal
                - api
                - mcp
                - keeper_internal
                - soul_grpc
                - background
                - config_bootstrap
              type: string
            type:
              - array
              - "null"
        - description: AID of the initiating Archon (case-insensitive substring, ILIKE)
          explode: false
          in: query
          name: archon_aid
          schema:
            description: AID of the initiating Archon (case-insensitive substring, ILIKE)
            type: string
        - description: ULID of the related event chain (case-insensitive substring, ILIKE)
          explode: false
          in: query
          name: correlation_id
          schema:
            description: ULID of the related event chain (case-insensitive substring, ILIKE)
            type: string
        - description: Herald channel name from payload->>'herald' (exact match)
          explode: false
          in: query
          name: payload_herald
          schema:
            description: Herald channel name from payload->>'herald' (exact match)
            type: string
        - description: voyage_id from payload->>'voyage_id' (exact match)
          explode: false
          in: query
          name: payload_voyage
          schema:
            description: voyage_id from payload->>'voyage_id' (exact match)
            type: string
        - description: created_at >= started_after (RFC3339, inclusive); bad-value -> 400
          explode: false
          in: query
          name: started_after
          schema:
            description: created_at >= started_after (RFC3339, inclusive); bad-value -> 400
            format: date-time
            type: string
        - description: created_at <= started_before (RFC3339, inclusive); bad-value -> 400
          explode: false
          in: query
          name: started_before
          schema:
            description: created_at <= started_before (RFC3339, inclusive); bad-value -> 400
            format: date-time
            type: string
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditEventListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Feed of audit-events (paged + filters)
      tags:
        - audit
  /v1/augur/omens:
    get:
      description: Registry of Omens with pagination (ADR-025). Permission omen.list. Read-only, no audit.
      operationId: listOmens
      parameters:
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OmenListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Omens (paged)
      tags:
        - augur
    post:
      description: Registers an Omen (external system) in the augur registry (ADR-025). Permission omen.create. 409 - name taken. master-credential is not stored (only auth_ref).
      operationId: createOmen
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OmenCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OmenView"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create Omen
      tags:
        - augur
  /v1/augur/omens/{name}:
    delete:
      description: Deletes an Omen cascadingly (related Rites, ADR-025). Permission omen.delete. 404 - record absent.
      operationId: deleteOmen
      parameters:
        - description: Omen name
          in: path
          name: name
          required: true
          schema:
            description: Omen name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete Omen
      tags:
        - augur
    get:
      description: Metadata of a single Omen by name (ADR-025). Permission omen.list (read is covered by the list permission). Read-only, no audit.
      operationId: getOmen
      parameters:
        - description: Omen name
          in: path
          name: name
          required: true
          schema:
            description: Omen name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OmenView"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Omen card
      tags:
        - augur
  /v1/augur/rites:
    get:
      description: Rites (grants) of a single Omen (ADR-025). Permission rite.list. Required filter omen=<name>. Read-only, no audit.
      operationId: listRites
      parameters:
        - description: by-omen filter (required in MVP); empty/broken -> 422
          explode: false
          in: query
          name: omen
          schema:
            description: by-omen filter (required in MVP); empty/broken -> 422
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiteListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Rites by Omen
      tags:
        - augur
    post:
      description: Registers a Rite (grant) in the augur registry (ADR-025). Permission rite.create. 404 - Omen does not exist. 422 - subject XOR violation/broken allow.
      operationId: createRite
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RiteCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiteView"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create Rite (grant)
      tags:
        - augur
  /v1/augur/rites/{id}:
    delete:
      description: Removes a Rite grant record by id (ADR-025). Permission rite.delete. 404 - record absent. 422 - id is not a positive number.
      operationId: deleteRite
      parameters:
        - description: numeric Rite id
          in: path
          name: id
          required: true
          schema:
            description: numeric Rite id
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete Rite
      tags:
        - augur
  /v1/cadences:
    get:
      description: Read-only list of Cadences with enabled/kind filters and pagination (sort created_at DESC). Permission cadence.list. Read-only, no audit.
      operationId: listCadences
      parameters:
        - description: "filter by enabled: true -> only enabled, false -> all (no filter); omitted -> all"
          explode: false
          in: query
          name: enabled
          schema:
            description: "filter by enabled: true -> only enabled, false -> all (no filter); omitted -> all"
            enum:
              - "true"
              - "false"
            type: string
        - description: filter by recipe type (exact); outside enum -> 422
          explode: false
          in: query
          name: kind
          schema:
            description: filter by recipe type (exact); outside enum -> 422
            enum:
              - scenario
              - command
            type: string
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CadenceListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of cadences (Cadence, paged)
      tags:
        - cadence
    post:
      description: "Regular/recurring Voyage (ADR-046). Two-tier RBAC: cadence.create + Voyage-permission by recipe kind."
      operationId: createCadence
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CadenceCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CadenceCreateReply"
          description: Created
          headers:
            Location:
              schema:
                type: string
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create schedule (Cadence)
      tags:
        - cadence
  /v1/cadences/{id}:
    delete:
      description: Deletes schedule; spawned Voyages remain (FK ON DELETE SET NULL). Permission cadence.delete.
      operationId: deleteCadence
      parameters:
        - description: ULID of schedule
          in: path
          name: id
          required: true
          schema:
            description: ULID of schedule
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete schedule (Cadence)
      tags:
        - cadence
    get:
      description: Schedule detail by ULID. Permission cadence.list. Read-only, no audit.
      operationId: getCadence
      parameters:
        - description: ULID of schedule
          in: path
          name: id
          required: true
          schema:
            description: ULID of schedule
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cadence"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Get schedule (Cadence)
      tags:
        - cadence
    patch:
      description: Read-modify-write recipe/schedule/enabled-toggle. Two-tier RBAC (cadence.update + Voyage-permission by kind). kind does not change.
      operationId: patchCadence
      parameters:
        - description: ULID of schedule
          in: path
          name: id
          required: true
          schema:
            description: ULID of schedule
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CadencePatchRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cadence"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Update schedule (Cadence)
      tags:
        - cadence
  /v1/cadences/{id}/disable:
    post:
      description: Pause scheduler. Permission cadence.disable OR backcompat cadence.update.
      operationId: disableCadence
      parameters:
        - description: ULID of schedule
          in: path
          name: id
          required: true
          schema:
            description: ULID of schedule
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CadenceEnabledReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Disable schedule (Cadence)
      tags:
        - cadence
  /v1/cadences/{id}/enable:
    post:
      description: Resume scheduler. Permission cadence.enable OR backcompat cadence.update.
      operationId: enableCadence
      parameters:
        - description: ULID of schedule
          in: path
          name: id
          required: true
          schema:
            description: ULID of schedule
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CadenceEnabledReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Enable schedule (Cadence)
      tags:
        - cadence
  /v1/cadences/{id}/runs:
    get:
      description: List of Voyages spawned by schedule, with status[] filter and pagination. Permission incarnation.history. Read-only, no audit.
      operationId: listCadenceRuns
      parameters:
        - description: ULID of schedule
          in: path
          name: id
          required: true
          schema:
            description: ULID of schedule
            type: string
        - description: multi-value ?status=X&status=Y — exact-match OR; value outside enum → 422
          explode: true
          in: query
          name: status
          schema:
            description: multi-value ?status=X&status=Y — exact-match OR; value outside enum → 422
            items:
              enum:
                - scheduled
                - pending
                - running
                - succeeded
                - failed
                - partial_failed
                - cancelled
              type: string
            type:
              - array
              - "null"
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoyageListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Schedule runs (Cadence runs, paged)
      tags:
        - cadence
  /v1/cluster:
    get:
      description: Live Keeper instances from the Conclave registry (kid + started_at + alive + is_reaper_leader) + self_kid + self_health (postgres/redis/vault of the current instance). Permission soul.list. Read-only, no audit. Agent (soul) version NOT included.
      operationId: getCluster
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClusterReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Keeper cluster HA topology
      tags:
        - cluster
  /v1/console/recordings:
    get:
      description: Recorded console sessions with filters and pagination (ADR-0074(g), NIM-145/NIM-148). Permission soul.console — the same right and the same selectors as opening the console itself; rows are narrowed to the hosts the caller may console into. Read-only, no audit.
      operationId: listConsoleRecordings
      parameters:
        - description: filter by recorded host (FQDN); malformed -> 422
          explode: false
          in: query
          name: sid
          schema:
            description: filter by recorded host (FQDN); malformed -> 422
            type: string
        - description: filter by the Archon whose session was recorded
          explode: false
          in: query
          name: archon_aid
          schema:
            description: filter by the Archon whose session was recorded
            type: string
        - description: interactive = a PTY session; command = a one-shot keeper.soul.run-command
          explode: false
          in: query
          name: kind
          schema:
            description: interactive = a PTY session; command = a one-shot keeper.soul.run-command
            enum:
              - interactive
              - command
            type: string
        - description: filter by start (started_at > value, RFC3339); bad value -> 400
          explode: false
          in: query
          name: started_after
          schema:
            description: filter by start (started_at > value, RFC3339); bad value -> 400
            format: date-time
            type: string
        - description: filter by start (started_at < value, RFC3339); bad value -> 400
          explode: false
          in: query
          name: started_before
          schema:
            description: filter by start (started_at < value, RFC3339); bad value -> 400
            format: date-time
            type: string
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsoleRecordingListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of recorded console sessions (paged)
      tags:
        - console
  /v1/console/recordings/{recording_id}:
    get:
      description: Metadata of one recorded session (ADR-0074(g)). Permission soul.console with the recording's host in scope. 404 covers both an unknown id and one outside the caller's scope. Read-only, no audit.
      operationId: getConsoleRecording
      parameters:
        - description: ULID of the recording
          in: path
          name: recording_id
          required: true
          schema:
            description: ULID of the recording
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsoleRecording"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Recorded console session (metadata)
      tags:
        - console
  /v1/console/recordings/{recording_id}/cast:
    get:
      description: The recorded session as an asciicast v2 file — a JSON header line followed by one JSON array per event (ADR-0074(g), NIM-145). Replays with asciinema, agg and xterm.js. Permission soul.console with the recording's host in scope; audited as console.recording-read. Vault references were masked when the session was recorded and are served as masked — there is no un-masked form of this body.
      operationId: getConsoleRecordingCast
      parameters:
        - description: ULID of the recording
          in: path
          name: recording_id
          required: true
          schema:
            description: ULID of the recording
            type: string
      responses:
        "200":
          content:
            application/x-asciicast:
              schema:
                type: string
          description: The asciicast v2 file.
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Recorded console session (asciicast v2)
      tags:
        - console
  /v1/decrees:
    get:
      description: Decree registry with pagination (ADR-030). Permission decree.list. Read-only, no audit.
      operationId: listDecrees
      parameters:
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecreeListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Decrees (paged)
      tags:
        - oracle
    post:
      description: Registers a Decree (reactor rule) in the oracle registry (ADR-030). Permission decree.create. 409 -- name taken.
      operationId: createDecree
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecreeCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecreeView"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create Decree
      tags:
        - oracle
  /v1/decrees/{name}:
    delete:
      description: Deletes a Decree cascading (cooldown state, ADR-030). Permission decree.delete. 404 -- record absent.
      operationId: deleteDecree
      parameters:
        - description: Decree name
          in: path
          name: name
          required: true
          schema:
            description: Decree name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete Decree
      tags:
        - oracle
    get:
      description: Metadata of a single Decree by name (ADR-030). Permission decree.list (read is covered by the list permission). Read-only, no audit.
      operationId: getDecree
      parameters:
        - description: Decree name
          in: path
          name: name
          required: true
          schema:
            description: Decree name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DecreeView"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Decree card
      tags:
        - oracle
  /v1/deprecations:
    get:
      description: "Which incarnations still pass a module param marked `deprecated:` in its manifest, and the release it stops working in (ADR-0076(v)). Sourced from the DEFINITIONS the incarnations are pinned to - NOT from run history, which would miss an incarnation nobody has run lately and still list one fixed yesterday. `items` is ordered by `removed_in`: what breaks first comes first. `gaps` lists what could not be checked (most often a plugin module, whose manifest ships beside its binary rather than with the definition) - an empty `items` with a non-empty `gaps` means \"not checked\", not \"clean\". Visibility scoped by RBAC exactly as the incarnation list (fail-closed: empty scope -> empty survey). Permission incarnation.list. Read-only."
      operationId: listDeprecations
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeprecationsReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Deprecated params still used across the estate
      tags:
        - deprecations
  /v1/errands:
    get:
      description: Registry of Errands with filters and pagination (ADR-033). Permission errand.list. Read-only, no audit.
      operationId: listErrands
      parameters:
        - description: filter by target Soul (FQDN); malformed -> 422
          explode: false
          in: query
          name: sid
          schema:
            description: filter by target Soul (FQDN); malformed -> 422
            type: string
        - description: filter by Errand status; value outside enum -> 422
          explode: false
          in: query
          name: status
          schema:
            description: filter by Errand status; value outside enum -> 422
            enum:
              - running
              - success
              - failed
              - timed_out
              - cancelled
              - module_not_allowed
            type: string
        - description: filter by start (started_at > value, RFC3339); bad value -> 400
          explode: false
          in: query
          name: started_after
          schema:
            description: filter by start (started_at > value, RFC3339); bad value -> 400
            format: date-time
            type: string
        - description: multi-value exact-match OR by module name (?module=X&module=Y)
          explode: false
          in: query
          name: module
          schema:
            description: multi-value exact-match OR by module name (?module=X&module=Y)
            items:
              type: string
            type:
              - array
              - "null"
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrandListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Errands (paged)
      tags:
        - errand
  /v1/errands/{errand_id}:
    delete:
      description: Sends a cancel signal to the Soul (ADR-033, slice E5). Permission errand.cancel. 409 - already terminal.
      operationId: cancelErrand
      parameters:
        - description: ULID of Errand
          in: path
          name: errand_id
          required: true
          schema:
            description: ULID of Errand
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Cancel Errand
      tags:
        - errand
    get:
      description: Terminal state (200) or running-poll (202) by ULID (ADR-033). Permission errand.list. Read-only, no audit.
      operationId: getErrand
      parameters:
        - description: ULID of Errand
          in: path
          name: errand_id
          required: true
          schema:
            description: ULID of Errand
            type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: OK
        "202":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Accepted
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Errand state
      tags:
        - errand
  /v1/event-types:
    get:
      description: "Valid event types for Tiding subscriptions: areas (area-glob `<name>.*`) + point-wise point_events (sourced from herald/eventtypes.go). Auth-only, no dedicated permission (self-describing). Read-only, no audit."
      operationId: listEventTypes
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventTypeCatalogReply"
          description: OK
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Event-types catalog for Tiding subscriptions
      tags:
        - event-type
  /v1/herald-types:
    get:
      description: "Notification channel types and their config fields (webhook/telegram/slack/mattermost/discord/custom/email): name/label/required/secret/kind. Sourced from herald.TypeCatalog (the same one which validates CRUD). Auth-only, no dedicated permission (self-describing). Read-only, no audit."
      operationId: listHeraldTypes
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeraldTypeCatalogReply"
          description: OK
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Herald channel types catalog
      tags:
        - herald
  /v1/heralds:
    get:
      description: Herald channel registry with pagination (ADR-052). Permission herald.list. Read-only, no audit.
      operationId: listHeralds
      parameters:
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HeraldListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List Herald channels (paged)
      tags:
        - herald
    post:
      description: Registers a Herald (notification delivery channel) in the registry (ADR-052). Permission herald.create. 409 — name taken. Secret is not stored (only secret_ref).
      operationId: createHerald
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HeraldCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Herald"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create Herald channel
      tags:
        - herald
  /v1/heralds/{name}:
    delete:
      description: Deletes the Herald cascadingly (related Tidings, ADR-052). Permission herald.delete. 404 — record absent.
      operationId: deleteHerald
      parameters:
        - description: Herald channel name
          in: path
          name: name
          required: true
          schema:
            description: Herald channel name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete Herald channel
      tags:
        - herald
    get:
      description: Metadata of a single Herald channel by name (ADR-052). Permission herald.read. Read-only, no audit.
      operationId: getHerald
      parameters:
        - description: Herald channel name
          in: path
          name: name
          required: true
          schema:
            description: Herald channel name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Herald"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Herald channel card
      tags:
        - herald
    put:
      description: "Replace semantics: fields fully replace the existing ones, name immutable (ADR-052). Permission herald.update. 404 — record absent."
      operationId: updateHerald
      parameters:
        - description: Herald channel name (immutable)
          in: path
          name: name
          required: true
          schema:
            description: Herald channel name (immutable)
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HeraldUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Herald"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Update Herald channel (replace)
      tags:
        - herald
  /v1/incarnations:
    get:
      description: Filters service/status/coven/state.<field> + sorting. Visibility scoped by RBAC (ADR-047). Permission incarnation.list. Read-only.
      operationId: listIncarnations
      parameters:
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
        - description: filter by service name
          explode: false
          in: query
          name: service
          schema:
            description: filter by service name
            type: string
        - description: filter by status (ready/applying/error_locked/migration_failed); invalid → 422
          explode: false
          in: query
          name: status
          schema:
            description: filter by status (ready/applying/error_locked/migration_failed); invalid → 422
            type: string
        - description: exact-match by covens[] (ADR-008); invalid label → 422
          explode: false
          in: query
          name: coven
          schema:
            description: exact-match by covens[] (ADR-008); invalid label → 422
            type: string
        - description: sort field (created_at/name/status/service or state.<field>)
          explode: false
          in: query
          name: sort
          schema:
            description: sort field (created_at/name/status/service or state.<field>)
            type: string
        - description: sort direction (asc/desc)
          explode: false
          in: query
          name: sort_dir
          schema:
            description: sort direction (asc/desc)
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List incarnations (paged)
      tags:
        - incarnation
    post:
      description: Service runtime instance (ADR-029). Runs create scenario (async, if lifecycle.auto_create). Permission incarnation.create.
      operationId: createIncarnation
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationCreateRequest"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationCreateReply"
          description: Accepted
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create incarnation
      tags:
        - incarnation
  /v1/incarnations/resolve-name:
    post:
      description: "Live preview for the create form: composes the incarnation name from the chosen create scenario's name_template over the input so far, reports its length against the ceiling, and whether the name is free. Composition runs server-side — the same code the create runs — so the previewed name cannot differ from the created one. Creates nothing. Permission incarnation.create; the composed name is measured against the caller's scope, and the occupying service is named only to a caller who may see it."
      operationId: resolveIncarnationName
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationResolveNameRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationResolveNameReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Resolve the name a create would compose
      tags:
        - incarnation
  /v1/incarnations/{name}:
    delete:
      description: allow_destroy=true -> DELETE without teardown; false -> scenario destroy (S-D4). Permission incarnation.destroy.
      operationId: destroyIncarnation
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: "confirmation-flag: true -> destroy without teardown"
          explode: false
          in: query
          name: allow_destroy
          required: true
          schema:
            description: "confirmation-flag: true -> destroy without teardown"
            type: boolean
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationDestroyReply"
          description: Accepted
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Destroy an incarnation
      tags:
        - incarnation
    get:
      description: Service runtime instance detail. Outside RBAC scope -> 404 (does not leak existence). Permission incarnation.get. Read-only.
      operationId: getIncarnation
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationGetReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Get incarnation
      tags:
        - incarnation
  /v1/incarnations/{name}/check-drift:
    post:
      description: Sync dry_run converge -> DriftReport (ADR-031 Slice B). Informational status=drift marking. Permission incarnation.check-drift.
      operationId: checkIncarnationDrift
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationCheckDriftRequest"
              description: "optional body: override converge parameters"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DriftReport"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Check incarnation drift (Scry)
      tags:
        - incarnation
  /v1/incarnations/{name}/choirs:
    get:
      description: Topology of Choirs of the incarnation (ADR-044). Permission choir.list. Nonexistent incarnation -> items=[]. Read-only, no audit.
      operationId: listChoirs
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChoirListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List Choirs of the incarnation
      tags:
        - choir
    post:
      description: Declared-topology of hosts inside the incarnation (ADR-044). created_by_aid from JWT. Permission choir.create. 409 - name taken.
      operationId: createChoir
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChoirCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Choir"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create Choir
      tags:
        - choir
  /v1/incarnations/{name}/choirs/{choir}:
    delete:
      description: Deletes Choir (cascading its Voices). Permission choir.delete.
      operationId: deleteChoir
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: Choir name
          in: path
          name: choir
          required: true
          schema:
            description: Choir name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete Choir
      tags:
        - choir
  /v1/incarnations/{name}/choirs/{choir}/voices:
    get:
      description: Members of the Choir (ADR-044). Permission choir.list. Nonexistent Choir -> items=[]. Read-only, no audit.
      operationId: listVoices
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: Choir name
          in: path
          name: choir
          required: true
          schema:
            description: Choir name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoiceListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List Voices of the Choir
      tags:
        - choir
    post:
      description: SID membership in the Choir (ADR-044). added_by_aid from JWT. Permission choir.add-voice. 409 - Voice already exists; 422 - SID is not a member of the incarnation.
      operationId: addVoice
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: Choir name
          in: path
          name: choir
          required: true
          schema:
            description: Choir name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VoiceAddRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Voice"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Add Voice to Choir
      tags:
        - choir
  /v1/incarnations/{name}/choirs/{choir}/voices/{sid}:
    delete:
      description: Removes SID membership in the Choir (ADR-044). Permission choir.remove-voice.
      operationId: removeVoice
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: Choir name
          in: path
          name: choir
          required: true
          schema:
            description: Choir name
            type: string
        - description: SID (FQDN) of a host
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of a host
            pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Remove Voice from Choir
      tags:
        - choir
  /v1/incarnations/{name}/history:
    get:
      description: state_history with apply_id filter and pagination. Outside RBAC scope -> 404. Permission incarnation.history. Read-only.
      operationId: getIncarnationHistory
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: opt. ULID filter by state_history.apply_id; non-ULID → 400
          explode: false
          in: query
          name: apply_id
          schema:
            description: opt. ULID filter by state_history.apply_id; non-ULID → 400
            type: string
        - description: opt. include the rerun-transition markers (excluded by default)
          explode: false
          in: query
          name: include_transitions
          schema:
            default: false
            description: opt. include the rerun-transition markers (excluded by default)
            type: boolean
        - description: opt. include soft-deleted (archived) snapshots (excluded by default)
          explode: false
          in: query
          name: include_archived
          schema:
            default: false
            description: opt. include soft-deleted (archived) snapshots (excluded by default)
            type: boolean
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationHistoryReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Incarnation state transition history (paged)
      tags:
        - incarnation
  /v1/incarnations/{name}/members:
    get:
      description: Member hosts of the incarnation (incarnation_membership, ADR-008 amendment / NIM-124), with bound_at / bound_by_aid. Permission incarnation.get. Narrowed to the hosts inside the caller's soul scope. Read-only, no audit.
      operationId: listIncarnationMembers
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationMemberListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List the incarnation's roster
      tags:
        - incarnation
    post:
      description: "Binds already-onboarded, connected Souls to the incarnation's roster so a scenario can subsequently roll onto them (ADR-008 amendment, NIM-209). Idempotent: re-binding a member is a no-op reported in already_member. Permission incarnation.bind-member; EVERY target SID must also be inside the caller's soul scope (all-or-nothing) - otherwise 403. 422 - unknown SID or a host that is not connected."
      operationId: bindIncarnationMembers
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationMemberBindRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationMemberBindReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Bind hosts to an incarnation
      tags:
        - incarnation
  /v1/incarnations/{name}/members/{sid}:
    delete:
      description: "Removes the host from the incarnation's roster - it stops being a target of every FUTURE run (ADR-008 amendment, NIM-209). Idempotent: unbinding a non-member succeeds unchanged. Permission incarnation.unbind-member; the SID must also be inside the caller's soul scope."
      operationId: unbindIncarnationMember
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: SID (FQDN) of the host to unbind
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of the host to unbind
            pattern: ^[a-z0-9][a-z0-9.-]{0,253}$
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Unbind a host from an incarnation
      tags:
        - incarnation
  /v1/incarnations/{name}/rerun-last:
    post:
      description: Clears error_locked and, in the same action, restarts the incarnation last failed scenario (bootstrap create/... or operational add_user/...) with the stored input of the failed run (one tx FOR UPDATE). Permission incarnation.rerun-last.
      operationId: rerunLastIncarnation
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationRerunLastRequest"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationRerunLastReply"
          description: Accepted
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Restart the last failed scenario from error_locked
      tags:
        - incarnation
  /v1/incarnations/{name}/runs:
    get:
      description: "Fold apply_runs by apply_id: run status (applying/success/failed/cancelled), time bounds, initiator. Run (apply_run) - NOT Voyage. Outside RBAC scope -> 404. Permission incarnation.history. Read-only."
      operationId: listIncarnationRuns
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationRunsReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List incarnation runs (paged)
      tags:
        - incarnation
  /v1/incarnations/{name}/runs/{apply_id}:
    get:
      description: "Slice by hosts of one apply_id: status per host + address of failed task (task_idx/plan_index/error). Foreign apply_id / outside RBAC scope -> 404. Permission incarnation.history. Read-only."
      operationId: getIncarnationRun
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: run ULID; non-ULID → 400
          in: path
          name: apply_id
          required: true
          schema:
            description: run ULID; non-ULID → 400
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunDetailReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Incarnation run details (per-host)
      tags:
        - incarnation
  /v1/incarnations/{name}/runs/{apply_id}/events:
    get:
      description: "text/event-stream: task.executed/apply.completed/failed/cancelled by apply_id. Auth: Authorization: Bearer (fetch-streaming, ADR-068 §A0). Access: the initiator OR incarnation.get/history; someone else's/nonexistent apply_id -> 403 (anti-enum, parity /mcp/events). Secrets in the payload are masked."
      operationId: streamIncarnationRunEvents
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: run ULID; someone else's/nonexistent -> 403 (anti-enum)
          in: path
          name: apply_id
          required: true
          schema:
            description: run ULID; someone else's/nonexistent -> 403 (anti-enum)
            type: string
      responses:
        "200":
          content:
            text/event-stream:
              schema:
                type: string
          description: SSE stream of apply events for the run
        "401":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unauthorized
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Live run progress of an incarnation (SSE)
      tags:
        - incarnation
  /v1/incarnations/{name}/runs/{apply_id}/tasks:
    get:
      description: Task plan of one apply_id (plan_index/name/module/no_log/passage) + per-host status/output/error from the audit log (task.executed) joined by plan_index. Foreign apply_id / outside RBAC scope -> 404. Permission incarnation.history. Read-only.
      operationId: getIncarnationRunTasks
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: run ULID; non-ULID → 400
          in: path
          name: apply_id
          required: true
          schema:
            description: run ULID; non-ULID → 400
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunTasksReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Incarnation run tasks (plan + per-host)
      tags:
        - incarnation
  /v1/incarnations/{name}/scenarios/{scenario}:
    post:
      description: Async run of a named scenario (ADR-009). Blocked on cluster:degraded (503). Permission incarnation.run.
      operationId: runIncarnationScenario
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: scenario name
          in: path
          name: scenario
          required: true
          schema:
            description: scenario name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationRunRequest"
              description: "opt. body: scenario input"
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationRunReply"
          description: Accepted
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "503":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Service Unavailable
      summary: Run incarnation scenario
      tags:
        - incarnation
  /v1/incarnations/{name}/scenarios/{scenario}/form-prefill:
    post:
      description: Current state values under scenario schema fields with prefill_from_state (docs/input.md). Path-whitelist (client does not set the path), secret fields excluded. Out of RBAC-scope → 404. Permission incarnation.get. Read-only, no audit.
      operationId: incarnationFormPrefill
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: scenario name
          in: path
          name: scenario
          required: true
          schema:
            description: scenario name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationFormPrefillReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Pre-fill scenario form from incarnation.state
      tags:
        - incarnation
  /v1/incarnations/{name}/secrets/reveal:
    post:
      description: Resolves the plaintext of a secret declared in the service's revealable_secrets, from Vault. Permission incarnation.view-secrets (removes the mask, strictly more privileged than incarnation.get). key must be in the current-state enumerate array. Audit incarnation.secret_revealed (without the value). Out of scope -> 404.
      operationId: incarnationRevealSecret
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationRevealSecretRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationRevealSecretReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Reveal plaintext of an incarnation secret
      tags:
        - incarnation
  /v1/incarnations/{name}/secrets/revealable:
    get:
      description: Discovery of the service's revealable_secrets + keys from the current-state enumerate array. Read-only, no audit. Permission incarnation.view-secrets (existence-gate). Out of scope -> 404.
      operationId: incarnationRevealableSecrets
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationRevealableSecretsReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List revealable secrets of an incarnation
      tags:
        - incarnation
  /v1/incarnations/{name}/telemetry:
    get:
      description: Aggregate of the latest utilization snapshots across incarnation hosts (latest+stale per host, without a window) from Redis (NIM-86). Permission incarnation.get; host visibility - soul-read-scope. Empty souls / out of scope -> hosts:[]. Read-only, without audit.
      operationId: getIncarnationTelemetry
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationTelemetryReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Host-vitals of incarnation hosts
      tags:
        - incarnation
  /v1/incarnations/{name}/traits:
    put:
      description: "Wholesale replacement of incarnation.traits (ADR-060) - the labels of the incarnation itself. Member hosts are not touched: a host carries only the traits an operator set on it (NIM-281). Permission incarnation.traits-set."
      operationId: setIncarnationTraits
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationSetTraitsRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationGetReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Replace operator-set trait labels of an incarnation
      tags:
        - incarnation
  /v1/incarnations/{name}/unlock:
    post:
      description: error_locked / migration_failed → ready under FOR UPDATE; state does not change (ADR-009/019). Permission incarnation.unlock.
      operationId: unlockIncarnation
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationUnlockRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationUnlockReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Remove incarnation blocking status
      tags:
        - incarnation
  /v1/incarnations/{name}/upgrade:
    post:
      description: Sync-under-202 migration state_schema (ADR-019) + service_version change in one tx. Permission incarnation.upgrade.
      operationId: upgradeIncarnation
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncarnationUpgradeRequest"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationUpgradeReply"
          description: Accepted
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Migrate incarnation to new version
      tags:
        - incarnation
  /v1/incarnations/{name}/upgrade-paths:
    get:
      description: Cheap list of service registry refs (marking is_current) without ?to=; on-demand analysis of one target (direction / found-legacy / state migrations) with ?to=<ref> (ADR-0068 6). Permission incarnation.upgrade (read facet). Read-only, no audit.
      operationId: getIncarnationUpgradePaths
      parameters:
        - description: incarnation name
          in: path
          name: name
          required: true
          schema:
            description: incarnation name
            type: string
        - description: opt. target git-ref for on-demand analysis of single target; empty → list of registry tags + is_current
          explode: false
          in: query
          name: to
          schema:
            description: opt. target git-ref for on-demand analysis of single target; empty → list of registry tags + is_current
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncarnationUpgradePathsReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: Incarnation upgrade paths
      tags:
        - incarnation
  /v1/me/permissions:
    get:
      description: Subset of the catalog actually granted to the current operator (AID from JWT-claims). Auth-only (any authenticated caller can see their own permissions; others' are not exposed). Read-only, no audit.
      operationId: listMyPermissions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyPermissionsReply"
          description: OK
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Effective permissions of the current Archon
      tags:
        - permission
  /v1/modules:
    get:
      description: Available modules to run (core + active plugin) + input metadata (ADR-045). Optional errand_safe filter. Permission service.list. Read-only, no audit.
      operationId: listModules
      parameters:
        - description: only modules with at least one errand-safe state (for the Run->Command whitelist)
          explode: false
          in: query
          name: errand_safe
          schema:
            description: only modules with at least one errand-safe state (for the Run->Command whitelist)
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModuleCatalogReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Module catalog
      tags:
        - module
  /v1/modules/{name}:
    get:
      description: Detail of one module by full name (ADR-045). Permission service.list. Read-only, no audit.
      operationId: getModule
      parameters:
        - description: full module name without the state suffix
          in: path
          name: name
          required: true
          schema:
            description: full module name without the state suffix
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModuleCatalogItem"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Module details
      tags:
        - module
  /v1/modules/{name}/form-prep:
    post:
      description: Live SIDs for the source fields of the Run->Command UI form (ADR-045 S3). Permission incarnation.run. Read-only resolve, no audit.
      operationId: moduleFormPrep
      parameters:
        - description: full module name (per-module contract, not used during resolve)
          in: path
          name: name
          required: true
          schema:
            description: full module name (per-module contract, not used during resolve)
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ModuleFormPrepRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModuleFormPrepReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Resolve the module form source catalog
      tags:
        - module
  /v1/operators:
    get:
      description: Registry of operators with filters (auth_method enum, revoked, q - substring search over display_name/aid, case-insensitive) and pagination. Permission operator.list. Read-only, no audit.
      operationId: listOperators
      parameters:
        - description: filter by credential form; value outside enum -> 422
          explode: false
          in: query
          name: auth_method
          schema:
            description: filter by credential form; value outside enum -> 422
            enum:
              - jwt
              - mtls
              - combined
              - ldap
              - oidc
            type: string
        - description: include revoked (false - active only); bad-value -> 400
          explode: false
          in: query
          name: revoked
          schema:
            description: include revoked (false - active only); bad-value -> 400
            type: boolean
        - description: free search over display_name/aid (substring, case-insensitive); empty -> no filter
          explode: false
          in: query
          name: q
          schema:
            description: free search over display_name/aid (substring, case-insensitive); empty -> no filter
            type: string
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Archons (paged + filters)
      tags:
        - operator
    post:
      description: Creates an operator (Archon) + opt. atomic-grant of roles (ADR-013/014). Permission operator.create. 409 - AID taken. Returns JWT once.
      operationId: createOperator
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OperatorCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorCreateReply"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create an Archon
      tags:
        - operator
  /v1/operators/{aid}:
    get:
      description: Metadata of a single operator by AID. Permission operator.list (read covered by list-right). Read-only, no audit.
      operationId: getOperator
      parameters:
        - description: AID of the Archon
          in: path
          name: aid
          required: true
          schema:
            description: AID of the Archon
            pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Operator"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Archon card
      tags:
        - operator
  /v1/operators/{aid}/issue-token:
    post:
      description: Issues a fresh JWT to an existing operator (ADR-014). Permission operator.issue-token. 409 - operator revoked. Returns JWT once.
      operationId: issueOperatorToken
      parameters:
        - description: AID of the Archon for whom a new JWT is issued
          in: path
          name: aid
          required: true
          schema:
            description: AID of the Archon for whom a new JWT is issued
            pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssueTokenReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Issue a new JWT to an Archon
      tags:
        - operator
  /v1/operators/{aid}/revoke:
    post:
      description: Sets operators.revoked_at (ADR-014). Permission operator.revoke. 409 - last cluster-admin (self-lockout protection) or already revoked.
      operationId: revokeOperator
      parameters:
        - description: AID of the Archon to revoke
          in: path
          name: aid
          required: true
          schema:
            description: AID of the Archon to revoke
            pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OperatorRevokeRequest"
        required: true
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Revoke an Archon
      tags:
        - operator
  /v1/permissions:
    get:
      description: Machine-readable catalog of `<resource>.<action>` (sourced from rbac.catalog.go), grouped by resource. Auth-only, no dedicated permission (self-describing). Read-only, no audit.
      operationId: listPermissions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PermissionCatalogReply"
          description: OK
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: RBAC permissions catalog
      tags:
        - permission
  /v1/plugins/sigils:
    get:
      description: Feed of active plugin releases (without signature/manifest, ADR-026 S4a). Permission plugin.list. Read-only, no audit.
      operationId: listPluginSigils
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PluginSigilListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List active Sigils
      tags:
        - plugin
    post:
      description: Registers (namespace,name,ref) in the plugin integrity allow-list with SHA-256 signature (ADR-026 S4a). Permission plugin.allow. 404 — plugin not in the host cache. 409 — release already active.
      operationId: allowPluginSigil
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PluginSigilAllowRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PluginSigilAllowReply"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Allow a plugin (Sigil)
      tags:
        - plugin
  /v1/plugins/sigils/{namespace}/{name}/{ref}:
    delete:
      description: Removes the active release (namespace,name,ref) from allow-list (ADR-026 S4a). Permission plugin.revoke. 404 — active record absent.
      operationId: revokePluginSigil
      parameters:
        - description: plugin namespace
          in: path
          name: namespace
          required: true
          schema:
            description: plugin namespace
            pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
            type: string
        - description: plugin name
          in: path
          name: name
          required: true
          schema:
            description: plugin name
            pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
            type: string
        - description: git-tag-ref of the release
          in: path
          name: ref
          required: true
          schema:
            description: git-tag-ref of the release
            pattern: ^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Revoke Sigil
      tags:
        - plugin
  /v1/provisioning-policy:
    get:
      description: "Current list of allowed operator CREATION methods (provisioning_allowed_methods, ADR-058 Part B). policy_set=false -> policy is unset (default: all methods allowed). Permission provisioning.read. Read-only, no audit."
      operationId: getProvisioningPolicy
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisioningPolicyReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Policy of operator creation methods
      tags:
        - provisioning
    put:
      description: Replace semantics for the list of allowed operator CREATION methods (provisioning_allowed_methods, ADR-058 Part B). Permission provisioning.update. 422 - empty list (anti-lockout) or method outside {user,ldap,oidc}. Gates ONLY operator creation; existing operators log in independent of the policy.
      operationId: updateProvisioningPolicy
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProvisioningPolicyUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisioningPolicyReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Change the operator creation methods policy
      tags:
        - provisioning
  /v1/push-providers:
    get:
      description: Registry of Push Providers with pagination and name_pattern filter (ADR-032 S7-2). Permission push-provider.list. Read-only, no audit.
      operationId: listPushProviders
      parameters:
        - description: LIKE-prefix filter by name (optional)
          explode: false
          in: query
          name: name_pattern
          schema:
            description: LIKE-prefix filter by name (optional)
            type: string
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushProviderListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List Push Providers (paged)
      tags:
        - push-provider
    post:
      description: Creates a Push Provider (per-provider env-payload, ADR-032 S7-2). Permission push-provider.create. 409 — name already taken. sensitive keys must be vault-refs.
      operationId: createPushProvider
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushProviderCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushProvider"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create a Push Provider
      tags:
        - push-provider
  /v1/push-providers/{name}:
    delete:
      description: Deletes a Push Provider record (ADR-032 S7-2). Permission push-provider.delete. 404 — record absent.
      operationId: deletePushProvider
      parameters:
        - description: Push Provider name
          in: path
          name: name
          required: true
          schema:
            description: Push Provider name
            pattern: ^[a-z][a-z0-9-]{0,62}$
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete a Push Provider
      tags:
        - push-provider
    get:
      description: Metadata of a single Push Provider by name (ADR-032 S7-2). Permission push-provider.read. Read-only, no audit.
      operationId: getPushProvider
      parameters:
        - description: Push Provider name
          in: path
          name: name
          required: true
          schema:
            description: Push Provider name
            pattern: ^[a-z][a-z0-9-]{0,62}$
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushProvider"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Push Provider card
      tags:
        - push-provider
    put:
      description: "Replace semantics: params fully replaces the existing set (ADR-032 S7-2). Permission push-provider.update. 404 — record absent."
      operationId: updatePushProvider
      parameters:
        - description: Push Provider name
          in: path
          name: name
          required: true
          schema:
            description: Push Provider name
            pattern: ^[a-z][a-z0-9-]{0,62}$
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushProviderUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushProvider"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Replace a Push Provider's params
      tags:
        - push-provider
  /v1/push-runs:
    get:
      description: Global registry of push runs with status/ssh_provider filters and pagination (UI-4). Permission incarnation.history. Read-only, no audit.
      operationId: listPushRuns
      parameters:
        - description: multi-value ?status=X&status=Y — exact-match OR; value outside enum → 422
          explode: true
          in: query
          name: status
          schema:
            description: multi-value ?status=X&status=Y — exact-match OR; value outside enum → 422
            items:
              enum:
                - pending
                - running
                - success
                - partial_failed
                - failed
                - cancelled
              type: string
            type:
              - array
              - "null"
        - description: exact-match on push_runs.ssh_provider
          explode: false
          in: query
          name: ssh_provider
          schema:
            description: exact-match on push_runs.ssh_provider
            type: string
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushRunListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List push runs (paged)
      tags:
        - push
  /v1/push/apply:
    post:
      description: Async push-orchestrator (Variant C, ADR-004 push-flow). 202 + apply_id, then poll GET /v1/push/{apply_id}. Permission push.apply. Blocked by Toll on cluster:degraded (503).
      operationId: pushApply
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushApplyRequest"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushApplyReply"
          description: Accepted
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Run a Destiny push over SSH
      tags:
        - push
  /v1/push/{apply_id}:
    get:
      description: Current state of a push run by apply_id (ADR-004 push-flow). Permission push.read. Read-only, no audit (recovery-friendly when degraded).
      operationId: pushGet
      parameters:
        - description: ULID of the push run
          in: path
          name: apply_id
          required: true
          schema:
            description: ULID of the push run
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushApplyView"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Push run status
      tags:
        - push
  /v1/roles:
    get:
      description: "Catalog of RBAC roles with full permissions and operator membership (ADR-022). Permission role.list. SCOPED TO THE CALLER: the answer holds only the roles the caller could grant themselves — a role's permission set and its operator list together are the cluster's privilege map, so role.list is not the right to read all of it. A caller holding an unrestricted `*` — or the explicit `role.list-all` right — sees every role. Each role comes both AS STORED (permissions/default_scope) and AS RESOLVED against its derivation chain (effective_permissions/effective_scope, ADR-078) — consumers must not re-derive inheritance from parent_role. Read-only, no audit."
      operationId: listRoles
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleListReply"
          description: OK
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List roles
      tags:
        - role
    post:
      description: Creates RBAC role with set of permissions (ADR-022). Permission role.create. Optional parent_role derives the role from another one (ADR-078), bounding it by that role. WITHOUT parent_role the role tracks nothing, so it additionally requires role.create-root — the default is to derive from a role you hold. 409 — name already taken; 404 — parent_role not found; 403 — beyond the parent, beyond the caller's own rights, or parentless without role.create-root.
      operationId: createRole
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RoleCreateRequest"
        required: true
      responses:
        "201":
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create role
      tags:
        - role
  /v1/roles/{name}:
    delete:
      description: "Deletes RBAC role cascade (permissions + membership). Permission role.delete, which is the right to reach this endpoint and not the reach itself: the caller must also be able to ADMINISTER the role, i.e. could grant what it grants (NIM-214). Every holder of a role administers the roles derived from it; a role granting nothing is administrable by anyone; a bare `*` covers everything. 403 — the role's rights are beyond the caller's; 409 — builtin/last-admin/has derived roles."
      operationId: deleteRole
      parameters:
        - description: role name
          in: path
          name: name
          required: true
          schema:
            description: role name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete role
      tags:
        - role
  /v1/roles/{name}/operators:
    post:
      description: Idempotent (repeat — no-op). Permission role.grant-operator. 404 — role/operator not found.
      operationId: grantRoleOperator
      parameters:
        - description: role name
          in: path
          name: name
          required: true
          schema:
            description: role name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GrantOperatorRequest"
        required: true
      responses:
        "204":
          description: No Content
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Bind operator to role
      tags:
        - role
  /v1/roles/{name}/operators/{aid}:
    delete:
      description: "Removes membership entry (name, aid). Permission role.revoke-operator, which is the right to reach this endpoint and not the reach itself: unbinding asks for what binding asks for — the caller must hold the role's effective rights (NIM-285). 403 — the role is beyond the caller's rights; 409 — last-admin lock-out."
      operationId: revokeRoleOperator
      parameters:
        - description: role name
          in: path
          name: name
          required: true
          schema:
            description: role name
            type: string
        - description: AID of operator member of role
          in: path
          name: aid
          required: true
          schema:
            description: AID of operator member of role
            pattern: ^[a-z0-9][a-z0-9._@-]{1,127}$
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Unbind operator from role
      tags:
        - role
  /v1/roles/{name}/permissions:
    patch:
      description: "Replace semantics: set completely replaces existing (ADR-022). Permission role.update, which is the right to reach this endpoint and not the reach itself: the caller must also be able to ADMINISTER the role, i.e. could grant what it currently grants (NIM-214) — so trimming a role beyond your rights is refused even though removing permissions grants nothing. default_scope/parent_role follow PATCH presence (omitted → untouched). 409 — builtin/last-admin/unconfirmed cascade; 403 — the role is beyond the caller's rights, the result exceeds the parent role (ADR-078), or the result would be a parentless role granting something without role.create-root."
      operationId: updateRolePermissions
      parameters:
        - description: role name
          in: path
          name: name
          required: true
          schema:
            description: role name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RolePermissionsUpdateRequest"
        required: true
      responses:
        "204":
          description: No Content
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Replace role permissions
      tags:
        - role
  /v1/runs:
    get:
      description: "Folds apply_runs by apply_id ACROSS ALL incarnations: run status (applying/success/failed/cancelled), owner incarnation, time bounds, initiator. A run (apply_run) is NOT a Voyage. Sorting by column - sort/sort_dir (stable tie-break on apply_id). Visibility scoped by RBAC (ADR-047, fail-closed: empty scope -> empty list). Permission incarnation.history. Read-only."
      operationId: listRuns
      parameters:
        - description: filter by aggregate run status (applying/success/failed/cancelled); invalid -> 422
          explode: false
          in: query
          name: status
          schema:
            description: filter by aggregate run status (applying/success/failed/cancelled); invalid -> 422
            type: string
        - description: filter by incarnation name; invalid name -> 422
          explode: false
          in: query
          name: incarnation
          schema:
            description: filter by incarnation name; invalid name -> 422
            type: string
        - description: filter by the incarnation's owner service (exact match); longer than 128 characters -> 422
          explode: false
          in: query
          name: service
          schema:
            description: filter by the incarnation's owner service (exact match); longer than 128 characters -> 422
            type: string
        - description: free-text search (substring, case-insensitive) over incarnation/scenario/service/started_by; longer than 128 characters -> 422
          explode: false
          in: query
          name: q
          schema:
            description: free-text search (substring, case-insensitive) over incarnation/scenario/service/started_by; longer than 128 characters -> 422
            type: string
        - description: "filter: run start time >= (RFC3339, inclusive); invalid -> 422"
          explode: false
          in: query
          name: started_after
          schema:
            description: "filter: run start time >= (RFC3339, inclusive); invalid -> 422"
            type: string
        - description: "filter: run start time <= (RFC3339, inclusive); invalid -> 422"
          explode: false
          in: query
          name: started_before
          schema:
            description: "filter: run start time <= (RFC3339, inclusive); invalid -> 422"
            type: string
        - description: sort field (started_at/finished_at/status/incarnation/service/scenario; default started_at); invalid -> 422
          explode: false
          in: query
          name: sort
          schema:
            description: sort field (started_at/finished_at/status/incarnation/service/scenario; default started_at); invalid -> 422
            type: string
        - description: sort direction (asc/desc; default desc); invalid -> 422
          explode: false
          in: query
          name: sort_dir
          schema:
            description: sort direction (asc/desc; default desc); invalid -> 422
            type: string
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..100 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..100 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunsListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Global run list (paged)
      tags:
        - runs
  /v1/runs/stats:
    get:
      description: "Run counters by aggregate status (total/applying/success/failed/cancelled) for all time and for the last 24 hours, within the RBAC scope (fail-closed: empty scope -> zero aggregate). Permission incarnation.history. Read-only."
      operationId: getRunsStats
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunsStatsReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Run summary counters
      tags:
        - runs
  /v1/services:
    get:
      description: Registry of Services (sort name ASC, ADR-028). Permission service.list. Read-only, no audit.
      operationId: listServices
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Services
      tags:
        - service
    post:
      description: Registers the Service in the service_registry (ADR-028). Permission service.register. 409 - name taken. 404 - caller AID missing from the operator registry.
      operationId: registerService
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceRegisterRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceView"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Register a Service
      tags:
        - service
  /v1/services/{name}:
    delete:
      description: Deletes the registry entry by name + invalidates caches (ADR-028). Permission service.deregister. 404 - entry absent.
      operationId: deregisterService
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Remove a Service from the registry
      tags:
        - service
    get:
      description: Metadata of a single registry entry by name (ADR-028). Permission service.list. Read-only, no audit.
      operationId: getService
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceView"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Service card
      tags:
        - service
    patch:
      description: Replace semantics for git/ref/refresh, name is immutable (ADR-028). Permission service.update. 404 - entry absent.
      operationId: updateService
      parameters:
        - description: Service name (immutable)
          in: path
          name: name
          required: true
          schema:
            description: Service name (immutable)
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceView"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Update a Service (replace mutable-fields)
      tags:
        - service
  /v1/services/{name}/compat:
    get:
      description: "Effective keeper-version window of the service (ADR-0076): the intersection of the `compat:` block declared by service.yml and by EVERY destiny the service pulls at its pinned ref - the narrowest wins. `entities[]` carries each contribution so a narrow bound is attributable to the artifact that set it; `effective_window` is null when nothing declares one (unbounded - existing services keep working). `status` is a backend catalog value the UI renders as-is: ok | unsupported (a run on this instance is rejected with keeper_version_unsupported) | not_enforced (this build carries no comparable version, e.g. 0.0.0-dev) | window_empty (the declarations do not overlap - an authoring error). `keeper_version` is the raw build string of the instance serving the request, `keeper_release` the release core actually compared - during a rolling upgrade instances differ, and enforcement belongs to the instance that renders. Permission service.list. Read-only, no audit. ETag=snapshot SHA1; If-None-Match -> 304. Cache-Control: immutable+year for a pinned commit-SHA ref, otherwise no-cache. 502 - loader failed (service or destiny repo unreachable)."
      operationId: getServiceCompat
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
        - description: optional git-ref override (omitted -> ref from registry)
          explode: false
          in: query
          name: ref
          schema:
            description: optional git-ref override (omitted -> ref from registry)
            type: string
        - description: "conditional GET: 304 if it matches ETag (snapshot SHA1)"
          in: header
          name: If-None-Match
          schema:
            description: "conditional GET: 304 if it matches ETag (snapshot SHA1)"
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceCompatReply"
          description: OK
          headers:
            Cache-Control:
              schema:
                type: string
            ETag:
              schema:
                type: string
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: engine-compat window of a Service (declared keeper versions) + this keeper's version
      tags:
        - service
  /v1/services/{name}/dependencies:
    get:
      description: Destiny building blocks + custom modules declared in service.yml with their own git refs, for the UI Service Detail (ADR-007/028). Permission service.list. Read-only, no audit. 502 - loader failed.
      operationId: listServiceDependencies
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
        - description: opt. git-ref override (omitted → ref from registry)
          explode: false
          in: query
          name: ref
          schema:
            description: opt. git-ref override (omitted → ref from registry)
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceDependenciesReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: git dependencies of a Service
      tags:
        - service
  /v1/services/{name}/directives:
    get:
      description: "Catalog of valid service directive names (vars.redis_directives, major.minor series map -> names) for the UI redis_settings editor (ADR-042). Permission service.list. Read-only, no audit. ?version=X.Y.Z narrows to the series. ETag=snapshot SHA1; If-None-Match -> 304. Cache-Control: immutable+year for a pinned commit-SHA ref, otherwise no-cache (branch/tag mutable - revalidation via ETag/304). Service without a catalog -> directives:{} + 200. 502 - loader failed."
      operationId: listServiceDirectives
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
        - description: opt. git-ref override (omitted → ref from registry)
          explode: false
          in: query
          name: ref
          schema:
            description: opt. git-ref override (omitted → ref from registry)
            type: string
        - description: opt. version (e.g. 8.2.2) - narrow the catalog to the major.minor series
          explode: false
          in: query
          name: version
          schema:
            description: opt. version (e.g. 8.2.2) - narrow the catalog to the major.minor series
            type: string
        - description: "conditional GET: 304 if it matches the ETag (snapshot SHA1)"
          in: header
          name: If-None-Match
          schema:
            description: "conditional GET: 304 if it matches the ETag (snapshot SHA1)"
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceDirectivesReply"
          description: OK
          headers:
            Cache-Control:
              schema:
                type: string
            ETag:
              schema:
                type: string
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: catalog of valid redis.conf directives by version
      tags:
        - service
  /v1/services/{name}/refs:
    get:
      description: List of git refs of the Service remote repository for the UI Upgrade-modal (ADR-028). Permission service.list. Read-only, no audit. 502 - git source unreachable.
      operationId: listServiceRefs
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRefsListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: git tags + branches of a Service
      tags:
        - service
  /v1/services/{name}/scenarios:
    get:
      description: List of scenarios from a materialized snapshot of the Service git repo for the UI Run-modal (ADR-028). Permission service.list. Read-only, no audit. 502 - loader failed.
      operationId: listServiceScenarios
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
        - description: opt. git-ref override (omitted → ref from registry)
          explode: false
          in: query
          name: ref
          schema:
            description: opt. git-ref override (omitted → ref from registry)
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceScenariosListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: scenario from a snapshot of the Service repo
      tags:
        - service
  /v1/services/{name}/state-schema:
    get:
      description: state_schema version + structure declaration + migration chain (metadata-only) for the UI Schema explorer (ADR-019/028). Permission service.list. Read-only, no audit. 502 - loader failed.
      operationId: listServiceStateSchema
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
        - description: opt. git-ref override (omitted → ref from registry)
          explode: false
          in: query
          name: ref
          schema:
            description: opt. git-ref override (omitted → ref from registry)
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceStateSchemaReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: state_schema metadata of a Service
      tags:
        - service
  /v1/services/{name}/telemetry:
    get:
      description: "Effective default (per-service, without an incarnation) host-vitals config of the service (enabled/interval_sec/collectors) from the manifest `telemetry:` + known_collectors (full allowed set for the UI, ADR-042 backend-driven, ADR-072). Permission service.list. Read-only, no audit. ETag=snapshot SHA1; If-None-Match -> 304. Cache-Control: immutable+year for pinned commit-SHA ref, otherwise no-cache (mutable branch/tag). A service without a telemetry block -> manifest defaults (enabled=true, interval_sec=30, all collectors) + 200. 502 - loader failed. Not to be confused with /v1/incarnations/{name}/telemetry (runtime host-vitals from Redis, NIM-86)."
      operationId: getServiceTelemetry
      parameters:
        - description: Service name
          in: path
          name: name
          required: true
          schema:
            description: Service name
            type: string
        - description: optional git-ref override (omitted -> ref from registry)
          explode: false
          in: query
          name: ref
          schema:
            description: optional git-ref override (omitted -> ref from registry)
            type: string
        - description: "conditional GET: 304 if it matches ETag (snapshot SHA1)"
          in: header
          name: If-None-Match
          schema:
            description: "conditional GET: 304 if it matches ETag (snapshot SHA1)"
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceTelemetryReply"
          description: OK
          headers:
            Cache-Control:
              schema:
                type: string
            ETag:
              schema:
                type: string
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
        "502":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Gateway
      summary: default host-vitals telemetry config of a Service + allowed collectors
      tags:
        - service
  /v1/settings:
    get:
      description: Every admitted runtime setting with its type, range bounds, built-in default, the EFFECTIVE value on this instance and its source (default < pg < file, ADR-0073 — the instance's own keeper.yml wins). Where the file shadows a cluster override the entry also carries cluster_value + overridden_locally. Renders the settings form without hardcoding the field list in the UI. Permission setting.read. Read-only, no audit.
      operationId: listSettings
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettingsCatalogReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Keeper runtime settings catalog
      tags:
        - settings
  /v1/settings/{key}:
    delete:
      description: Removes the keeper_settings row, so the keeper.yml value (or the built-in default) is back in effect cluster-wide — the clean revert of ADR-0073(f). Permission setting.delete. 404 - the key is not in the registry, or there is no override to drop.
      operationId: deleteSetting
      parameters:
        - description: setting key whose override is dropped
          in: path
          name: key
          required: true
          schema:
            description: setting key whose override is dropped
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettingReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Drop a Keeper runtime setting override
      tags:
        - settings
    put:
      description: Writes a cluster-wide override into keeper_settings and invalidates the other Keeper nodes, which re-merge it onto their keeper.yml without a restart (ADR-0073). An instance whose own keeper.yml sets the key keeps its local value; the catalog then reports cluster_value + overridden_locally. Permission setting.update. 404 - the key is not in the registry; 422 - unparsable value, out of range, or a cross-field invariant broken either here or on a node whose file is silent about the key; nothing is written.
      operationId: updateSetting
      parameters:
        - description: setting key from GET /v1/settings (cfg_* namespace of keeper_settings)
          in: path
          name: key
          required: true
          schema:
            description: setting key from GET /v1/settings (cfg_* namespace of keeper_settings)
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SettingUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettingReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Override a Keeper runtime setting
      tags:
        - settings
  /v1/sigil/keys:
    get:
      description: Active trust-anchor signing keys (primary first, without vault_ref, ADR-026(h)). Permission sigil.key-list. Read-only, no audit.
      operationId: listSigilKeys
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SigilKeyListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of active Sigil signing keys
      tags:
        - sigil-key
    post:
      description: Generates an ed25519 pair, writes the private key into Vault, introduces the public part into the trust-anchor registry (ADR-026(h)). Permission sigil.key-introduce. Returns pubkey, NOT the private key.
      operationId: introduceSigilKey
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SigilKeyIntroduceRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SigilKeyIntroduceReply"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Introduce a Sigil signing key
      tags:
        - sigil-key
  /v1/sigil/keys/{key_id}:
    delete:
      description: Marks the key retired (ADR-026(h)). Permission sigil.key-retire. 404 - no active record; 409 - last active or primary (SetPrimary to another key first).
      operationId: retireSigilKey
      parameters:
        - description: key_id of the signing key to retire (SHA-256(SPKI), 64 hex)
          in: path
          name: key_id
          required: true
          schema:
            description: key_id of the signing key to retire (SHA-256(SPKI), 64 hex)
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Retire a signing key from active
      tags:
        - sigil-key
  /v1/sigil/keys/{key_id}/primary:
    post:
      description: Sets an active key as primary (new Sigils are signed with it, ADR-026(h)). Permission sigil.key-set-primary. 404 - key not found; 409 - retired/race.
      operationId: setPrimarySigilKey
      parameters:
        - description: key_id of the signing key (SHA-256(SPKI), 64 hex)
          in: path
          name: key_id
          required: true
          schema:
            description: key_id of the signing key (SHA-256(SPKI), 64 hex)
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Make a signing key primary
      tags:
        - sigil-key
  /v1/souls:
    get:
      description: Registry of souls with scoped visibility (ADR-047) and coven/status/transport filters. offset-fast-path or keyset (mode chosen server-side from Purview). Permission soul.list. Read-only, no audit.
      operationId: listSouls
      parameters:
        - description: filter by Coven label the host carries itself; belonging to an incarnation attaches no label (NIM-281); repeatable — matches ANY of the labels; AND within scope
          explode: false
          in: query
          name: coven
          schema:
            description: filter by Coven label the host carries itself; belonging to an incarnation attaches no label (NIM-281); repeatable — matches ANY of the labels; AND within scope
            items:
              type: string
            type:
              - array
              - "null"
        - description: filter by status; outside enum -> 422
          explode: false
          in: query
          name: status
          schema:
            description: filter by status; outside enum -> 422
            enum:
              - pending
              - connected
              - disconnected
              - revoked
              - expired
              - destroyed
            type: string
        - description: filter by transport; outside enum -> 422
          explode: false
          in: query
          name: transport
          schema:
            description: filter by transport; outside enum -> 422
            enum:
              - agent
              - ssh
            type: string
        - description: only hosts belonging to NO incarnation (incarnation_membership, NIM-124) — the free souls a create scenario can be rolled onto
          explode: false
          in: query
          name: unassigned
          schema:
            description: only hosts belonging to NO incarnation (incarnation_membership, NIM-124) — the free souls a create scenario can be rolled onto
            type: boolean
        - description: only SIDs starting with this prefix (autocomplete); matched literally, LIKE metacharacters included
          explode: false
          in: query
          name: sid_prefix
          schema:
            description: only SIDs starting with this prefix (autocomplete); matched literally, LIKE metacharacters included
            maxLength: 254
            type: string
        - description: keyset continuation cursor (regex-mode scope)
          explode: false
          in: query
          name: cursor
          schema:
            description: keyset continuation cursor (regex-mode scope)
            type: string
        - description: offset from start of set, ≥0 (out-of-range → 400; offset+cursor → 422)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400; offset+cursor → 422)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Souls (paged, scoped)
      tags:
        - soul
    post:
      description: "Onboarding host to souls registry (status: pending). For transport=agent, bootstrap token is issued. Permission soul.create. 409 — SID taken."
      operationId: createSoul
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoulCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulCreateReply"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Register Soul
      tags:
        - soul
  /v1/souls/coven:
    post:
      description: Bulk append/remove single label or replace set on hosts under selector ∩ scope (ADR-008). Permission soul.coven-assign. partial → 200 status:partial.
      operationId: assignSoulCoven
      parameters:
        - description: count matched without UPDATE (OR with body.dry_run)
          explode: false
          in: query
          name: dry_run
          schema:
            description: count matched without UPDATE (OR with body.dry_run)
            type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoulCovenAssignRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulCovenAssignReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Bulk assign Coven tags
      tags:
        - soul
  /v1/souls/stats:
    get:
      description: Summary by status/transport/coven + total + stale_count for Souls Overview with scoped visibility (ADR-047). transport - agent/ssh (UI maps to pull/push). stale_count - by mark_disconnected.stale_after. Permission soul.list. Read-only, no audit.
      operationId: getSoulsStats
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulStatsReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Souls registry aggregate (Overview)
      tags:
        - soul
  /v1/souls/traits:
    post:
      description: "Bulk merge/replace/remove of operator-set trait-tags attached to HOSTS (souls.traits jsonb) on hosts under selector ∩ coven-scope. These are a host's whole set of traits: belonging to an incarnation attaches nothing (NIM-281), so a host carries exactly the pairs an operator put on it. Permission soul.traits-assign; merge/replace additionally require every pair to lie inside the operator's own trait-scope (gate b). partial -> 200 status:partial."
      operationId: assignSoulTraits
      parameters:
        - description: count matched without UPDATE (OR with body.dry_run)
          explode: false
          in: query
          name: dry_run
          schema:
            description: count matched without UPDATE (OR with body.dry_run)
            type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoulTraitsAssignRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulTraitsAssignReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Bulk assignment of trait-tags to hosts
      tags:
        - soul
  /v1/souls/{sid}:
    get:
      description: One registry row of souls for the detail page (ADR-047 scoped). Permission soul.list. Outside scope -> 404. Read-only, no audit.
      operationId: getSoul
      parameters:
        - description: SID (FQDN) of Soul
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of Soul
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulListEntry"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Soul card
      tags:
        - soul
  /v1/souls/{sid}/exec:
    post:
      description: Pull ad-hoc module exec on a single host (ADR-033). 200 sync (terminal up to server-cap 30s) or 202 + Location async-escalation. Permission errand.run. 404 - Soul not connected.
      operationId: ErrandExec
      parameters:
        - description: SID (FQDN) of the target Soul
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of the target Soul
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrandRunRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: OK
          headers:
            Location:
              schema:
                type: string
        "202":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Accepted
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Run Errand on a Soul
      tags:
        - Errand
  /v1/souls/{sid}/history:
    get:
      description: Per-host timeline (scenario apply_runs + ad-hoc errands) with a scope gate. Permission soul.list. Read-only, no audit.
      operationId: getSoulHistory
      parameters:
        - description: SID (FQDN) of Soul
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of Soul
            type: string
        - description: multi-value ?type=X&type=Y - OR by source; outside enum -> 422
          explode: true
          in: query
          name: type
          schema:
            description: multi-value ?type=X&type=Y - OR by source; outside enum -> 422
            items:
              enum:
                - scenario
                - errand
              type: string
            type:
              - array
              - "null"
        - description: started_at > since (RFC3339); bad value → 400
          explode: false
          in: query
          name: since
          schema:
            description: started_at > since (RFC3339); bad value → 400
            format: date-time
            type: string
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulHistoryReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Soul run history (paged)
      tags:
        - soul
  /v1/souls/{sid}/issue-token:
    post:
      description: Reissue of the bootstrap token for transport=agent (?force=true expires the active one). Permission soul.issue-token. 409 - active token without force.
      operationId: issueSoulToken
      parameters:
        - description: SID (FQDN) of Soul
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of Soul
            type: string
        - description: expire the active token and issue a new one
          explode: false
          in: query
          name: force
          schema:
            description: expire the active token and issue a new one
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulIssueTokenReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Reissue bootstrap token
      tags:
        - soul
  /v1/souls/{sid}/soulprint:
    get:
      description: The latest typed SoulprintReport (ADR-018) with a scope gate. Permission soul.list. 410 - soulprint has never arrived. Read-only, no audit.
      operationId: getSoulprint
      parameters:
        - description: SID (FQDN) of Soul
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of Soul
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulprintReadReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "410":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Gone
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Soul soulprint
      tags:
        - soul
  /v1/souls/{sid}/ssh-target:
    put:
      description: Per-host SSH requisites for push-flow (ADR-032 S7-1). Replace semantics (full set). Permission soul.ssh-target-update. 404 - no soul.
      operationId: updateSoulSSHTarget
      parameters:
        - description: SID (FQDN) of Soul
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of Soul
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoulSshTarget"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulSshTargetReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Update Soul SSH requisites
      tags:
        - soul
  /v1/souls/{sid}/telemetry:
    get:
      description: Latest host utilization snapshot (CPU/load/mem/disk) + a window for sparklines from Redis (NIM-86, ADR-006), with a scope gate. Permission soul.list. stale=true if the snapshot is stale or there is no data (old agent -> graceful). Read-only, no audit.
      operationId: getSoulTelemetry
      parameters:
        - description: SID (FQDN) of the Soul
          in: path
          name: sid
          required: true
          schema:
            description: SID (FQDN) of the Soul
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SoulTelemetryReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Host-vitals of a Soul
      tags:
        - soul
  /v1/synods:
    get:
      description: "Catalog of Synod groups with role bundle and member roster (ADR-049). Permission synod.list. SCOPED TO THE CALLER: the answer holds only the groups the caller could add someone to — a group's bundle and its roster together say which packages of privilege exist and who holds them, so synod.list is not the right to read all of it. A group is visible when the caller covers the effective rights of EVERY role it bundles (the ADR-049(f) add-operator rule, read backwards); a group that bundles nothing is visible to everyone, and a visible group comes back whole. A caller holding an unrestricted `*` — or the explicit `synod.list-all` right — sees every group. Read-only, no audit."
      operationId: listSynods
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SynodListReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List Synod groups
      tags:
        - synod
    post:
      description: Registers a Synod group (a group of archons, a role bundle) in the registry (ADR-049). Permission synod.create. 409 - name taken.
      operationId: createSynod
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SynodCreateRequest"
        required: true
      responses:
        "201":
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create a Synod group
      tags:
        - synod
  /v1/synods/{name}:
    delete:
      description: Deletes the Synod cascadingly (membership + bundle, ADR-049). Permission synod.delete. 409 - builtin/last-admin lock-out.
      operationId: deleteSynod
      parameters:
        - description: Synod group name
          in: path
          name: name
          required: true
          schema:
            description: Synod group name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete a Synod group
      tags:
        - synod
    patch:
      description: Changes ONLY the description (name immutable, ADR-049 amend). Permission synod.update. 404 - entry absent.
      operationId: updateSynod
      parameters:
        - description: Synod group name (immutable)
          in: path
          name: name
          required: true
          schema:
            description: Synod group name (immutable)
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SynodUpdateRequest"
        required: true
      responses:
        "204":
          description: No Content
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Update a Synod group description
      tags:
        - synod
  /v1/synods/{name}/operators:
    post:
      description: The member receives the group's full role bundle (ADR-049). Idempotent. Permission synod.add-operator. 403 - least-privilege.
      operationId: addSynodOperator
      parameters:
        - description: Synod group name
          in: path
          name: name
          required: true
          schema:
            description: Synod group name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GrantOperatorRequest"
        required: true
      responses:
        "204":
          description: No Content
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Add an archon to a Synod group
      tags:
        - synod
  /v1/synods/{name}/operators/{aid}:
    delete:
      description: "Removes the membership row (name, aid). Permission synod.remove-operator, which is the right to reach this endpoint and not the reach itself: a member holds the group's WHOLE bundle, so removing one is measured against all of it, exactly as adding one is (ADR-049(f), NIM-285). 403 — the bundle is beyond the caller's rights; 409 - last-admin lock-out."
      operationId: removeSynodOperator
      parameters:
        - description: Synod group name
          in: path
          name: name
          required: true
          schema:
            description: Synod group name
            type: string
        - description: AID of the group member archon
          in: path
          name: aid
          required: true
          schema:
            description: AID of the group member archon
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Remove an archon from a Synod group
      tags:
        - synod
  /v1/synods/{name}/roles:
    post:
      description: All group members receive the role's effective permissions (ADR-049). Idempotent. Permission synod.grant-role. 403 - least-privilege.
      operationId: grantSynodRole
      parameters:
        - description: Synod group name
          in: path
          name: name
          required: true
          schema:
            description: Synod group name
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SynodGrantRoleRequest"
        required: true
      responses:
        "204":
          description: No Content
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Add a role to a Synod group bundle
      tags:
        - synod
  /v1/synods/{name}/roles/{role_name}:
    delete:
      description: "The role's permissions are revoked from all group members (ADR-049). Permission synod.revoke-role, which is the right to reach this endpoint and not the reach itself: the caller must hold the revoked role's effective rights, the same set synod.grant-role demands to put it in the bundle (NIM-285). 403 — the role is beyond the caller's rights; 409 - last-admin lock-out."
      operationId: revokeSynodRole
      parameters:
        - description: Synod group name
          in: path
          name: name
          required: true
          schema:
            description: Synod group name
            type: string
        - description: role name in the group bundle
          in: path
          name: role_name
          required: true
          schema:
            description: role name in the group bundle
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Remove a role from a Synod group bundle
      tags:
        - synod
  /v1/tidings:
    get:
      description: Tiding rule registry with pagination (ADR-052). Permission tiding.list. Hides ephemeral rules by default (include_ephemeral=true returns all). Read-only, no audit.
      operationId: listTidings
      parameters:
        - description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (matches shared/api.ParsePage; out-of-range → 400)
            format: int32
            type: integer
        - description: include ephemeral (one-off) rules (debugging); omitted → false hides ephemeral ones (ADR-052(g))
          explode: false
          in: query
          name: include_ephemeral
          schema:
            default: false
            description: include ephemeral (one-off) rules (debugging); omitted → false hides ephemeral ones (ADR-052(g))
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TidingListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List Tiding rules (paged)
      tags:
        - tiding
    post:
      description: Registers a permanent Tiding subscription rule (ADR-052). Permission tiding.create. 404 — Herald channel does not exist. 409 — name taken.
      operationId: createTiding
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TidingCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tiding"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create Tiding rule
      tags:
        - tiding
  /v1/tidings/{name}:
    delete:
      description: Removes the Tiding subscription rule by name (ADR-052). Permission tiding.delete. 404 — record absent.
      operationId: deleteTiding
      parameters:
        - description: Tiding rule name
          in: path
          name: name
          required: true
          schema:
            description: Tiding rule name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete Tiding rule
      tags:
        - tiding
    get:
      description: Metadata of a single Tiding rule by name (ADR-052). Permission tiding.read. Read-only, no audit.
      operationId: getTiding
      parameters:
        - description: Tiding rule name
          in: path
          name: name
          required: true
          schema:
            description: Tiding rule name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tiding"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Tiding rule card
      tags:
        - tiding
    put:
      description: "Replace semantics: fields fully replace the existing ones, name immutable (ADR-052). Permission tiding.update. 404 — record/Herald absent."
      operationId: updateTiding
      parameters:
        - description: Tiding rule name (immutable)
          in: path
          name: name
          required: true
          schema:
            description: Tiding rule name (immutable)
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TidingUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tiding"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Update Tiding rule (replace)
      tags:
        - tiding
  /v1/vigils:
    get:
      description: Vigil registry with pagination (ADR-030). Permission vigil.list. Read-only, no audit.
      operationId: listVigils
      parameters:
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VigilListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List of Vigils (paged)
      tags:
        - oracle
    post:
      description: Registers a Vigil (Soul-side check) in the oracle registry (ADR-030). Permission vigil.create. 409 -- name taken.
      operationId: createVigil
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VigilCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VigilView"
          description: Created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create Vigil
      tags:
        - oracle
  /v1/vigils/{name}:
    delete:
      description: Deletes a Vigil from the oracle registry (ADR-030). Permission vigil.delete. 404 -- record absent.
      operationId: deleteVigil
      parameters:
        - description: Vigil name
          in: path
          name: name
          required: true
          schema:
            description: Vigil name
            type: string
      responses:
        "204":
          description: No Content
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Delete Vigil
      tags:
        - oracle
    get:
      description: Metadata of a single Vigil by name (ADR-030). Permission vigil.list (read is covered by the list permission). Read-only, no audit.
      operationId: getVigil
      parameters:
        - description: Vigil name
          in: path
          name: name
          required: true
          schema:
            description: Vigil name
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VigilView"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Vigil card
      tags:
        - oracle
  /v1/voyages:
    get:
      description: List of runs with kind/status filters and pagination (ADR-043). target_resolved is NOT exposed (UI reads scope_size). Permission incarnation.history. Read-only, no audit.
      operationId: listVoyages
      parameters:
        - description: filter by kind; outside enum -> 422
          explode: false
          in: query
          name: kind
          schema:
            description: filter by kind; outside enum -> 422
            enum:
              - scenario
              - command
            type: string
        - description: multi-value ?status=X&status=Y OR; outside enum -> 422
          explode: true
          in: query
          name: status
          schema:
            description: multi-value ?status=X&status=Y OR; outside enum -> 422
            items:
              enum:
                - scheduled
                - pending
                - running
                - succeeded
                - failed
                - partial_failed
                - cancelled
              type: string
            type:
              - array
              - "null"
        - description: offset from start of set, ≥0 (out-of-range → 400)
          explode: false
          in: query
          name: offset
          schema:
            default: 0
            description: offset from start of set, ≥0 (out-of-range → 400)
            format: int32
            type: integer
        - description: page size 1..1000 (out-of-range → 400)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: page size 1..1000 (out-of-range → 400)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoyageListReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: List Voyage runs (paged)
      tags:
        - voyage
    post:
      description: "Unified batch run (ADR-043). RBAC-by-kind: scenario->incarnation.run, command->errand.run (fail-closed, in the handler). Tempo per-AID rate-limit."
      operationId: createVoyage
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VoyageCreateRequest"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoyageCreateReply"
          description: Accepted
          headers:
            Location:
              schema:
                type: string
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Create a Voyage
      tags:
        - voyage
  /v1/voyages/preview:
    post:
      description: Preview of the number of units/batches WITHOUT creating a Voyage (ADR-043 amendment 4). Same validation/resolve/RBAC as Create. Without revealing the SID list. Read-like - no audit.
      operationId: previewVoyage
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VoyageCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoyagePreviewReply"
          description: OK
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Bad Request
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "429":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Too Many Requests
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Dry-resolve scope Voyage
      tags:
        - voyage
  /v1/voyages/{id}:
    delete:
      description: Cancel pending/scheduled (running-abort - post-MVP). RBAC-by-kind in the handler. 409 - running/terminal. Permission by kind.
      operationId: cancelVoyage
      parameters:
        - description: ULID of Voyage run
          in: path
          name: id
          required: true
          schema:
            description: ULID of Voyage run
            type: string
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoyageCancelReply"
          description: Accepted
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Conflict
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Cancel a Voyage run
      tags:
        - voyage
    get:
      description: Detail + summary of a single run (ADR-043). Permission incarnation.history. Read-only, no audit.
      operationId: getVoyage
      parameters:
        - description: ULID of Voyage run
          in: path
          name: id
          required: true
          schema:
            description: ULID of Voyage run
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Voyage"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: Snapshot of a Voyage run
      tags:
        - voyage
  /v1/voyages/{id}/targets:
    get:
      description: Per-target batch/status/back-link of a single run (ADR-043). Permission incarnation.history. Read-only, no audit.
      operationId: listVoyageTargets
      parameters:
        - description: ULID of Voyage run
          in: path
          name: id
          required: true
          schema:
            description: ULID of Voyage run
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoyageTargetsReply"
          description: OK
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Forbidden
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Not Found
        "422":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Unprocessable Entity
        "500":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HumaProblemError"
          description: Internal Server Error
      summary: All-runs drill for a Voyage run
      tags:
        - voyage
security:
  - bearerAuth: []
